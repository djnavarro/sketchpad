% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curve_twist.R
\name{curve_twist}
\alias{curve_twist}
\title{An open, wandering path following a random walk}
\usage{
curve_twist(
  x = 0,
  y = 0,
  xend = 1,
  yend = 1,
  scale = 0.2,
  n = 100L,
  path_distortion = noise_bridge(),
  trans = trans_identity(),
  ...
)
}
\arguments{
\item{x, y}{Start point. Default \code{0}.}

\item{xend, yend}{End point. Default \code{1}.}

\item{scale}{Amplitude of the Brownian-bridge displacement (internally
scaled by \code{0.1}, matching \code{\link[=shape_twist]{shape_twist()}}'s own scaling of its
path). Must be non-negative. Default \code{0.2}.}

\item{n}{Number of points used along the path. Default \code{100L}.}

\item{path_distortion}{A \link{noise_bridge} controlling the path's
Brownian bridge. Default \code{noise_bridge()}.}

\item{trans}{A \link{trans} object applied to the curve's computed points.
Default \code{\link[=trans_identity]{trans_identity()}} (no transform).}

\item{...}{Arguments passed to \code{\link[=style]{style()}}.}
}
\description{
\code{curve_twist} is \code{\link[=shape_twist]{shape_twist()}}'s path alone, with no ribbon width:
an open \code{\link[=curve_line]{curve_line()}}-like polyline between \verb{(x, y)} and
\verb{(xend, yend)}, displaced away from a straight line by a
\link{noise_bridge}, giving a wandering, twisted appearance. Where
\code{shape_twist()} also modulates a filled ribbon's width along this same
kind of path (via a separate \code{distortion} \link{noise_field}), \code{curve_twist()}
has no width or fill at all -- just the displaced backbone itself,
rendered as an open \code{grid::polylineGrob()} (\code{geometry = "path"}).
Shares its path computation with \code{shape_twist()} via the internal
\code{twisted_path_points()} helper (\code{R/shape_twist.R}) rather than
duplicating it.
}
\details{
\code{style@fill} has no effect for \code{curve_twist()} -- see \link{drawable}'s
\code{geometry} documentation for why \code{"path"} geometries have no interior
to fill. Passing \code{fill} via \code{...} is still accepted (it's simply
ignored at draw time), since \code{style()} is shared across every
\code{geometry}.
}
\examples{
draw(curve_twist(
  x = 0, y = 0, xend = 1, yend = 0,
  path_distortion = noise_bridge(seed = 7734L)
))

}
\seealso{
Other 1D curves:
\code{\link[=curve_arc]{curve_arc()}},
\code{\link[=curve_bezier]{curve_bezier()}},
\code{\link[=curve_line]{curve_line()}},
\code{\link[=curve_raw]{curve_raw()}},
\code{\link[=curve_scribble]{curve_scribble()}},
\code{\link[=curve_spiral]{curve_spiral()}}
}
\concept{1D curves}
