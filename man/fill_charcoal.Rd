% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.R
\name{fill_charcoal}
\alias{fill_charcoal}
\title{Charcoal/marker-style noise texture fill}
\usage{
fill_charcoal(
  color = "gray15",
  spacing = 0.25,
  aspect = 1,
  resolution = 32L,
  alpha = 1,
  frequency = 4,
  octaves = 3L,
  seed = 1L,
  extend = "repeat"
)
}
\arguments{
\item{color}{One or more fill colours, as in \code{\link[=fill_noise]{fill_noise()}}. Default
\code{"gray15"} (lighter than \code{\link[=fill_noise]{fill_noise()}}'s own \code{"black"} default, closer
to a charcoal tone).}

\item{spacing}{Baseline tile size, as a fraction of the target's
bounding box. Must be a positive number. Default \code{0.25} (finer than
\code{\link[=fill_noise]{fill_noise()}}'s own \code{0.5} default, for denser grain).}

\item{aspect}{Width-to-height ratio of the target polygon's bounding box.
Must be a positive number. Default \code{1} (a square bounding box).}

\item{resolution}{Raster resolution (pixels per tile edge). Must be a
positive integer of at least \code{2L}. Default \code{32L}.}

\item{alpha}{Opacity. For a single \code{color}, the maximum opacity at the
noise field's peak; for two or more, a flat opacity applied uniformly.
Must be a number in \verb{(0, 1]}. Default \code{1}.}

\item{frequency}{Noise frequency, as in \code{\link[=shape_blob]{shape_blob()}}. Must be
non-negative. Default \code{4} (finer detail than \code{\link[=fill_noise]{fill_noise()}}'s own
default of \code{1}).}

\item{octaves}{Number of noise octaves, as in \code{\link[=shape_blob]{shape_blob()}}. Must be a
positive integer. Default \code{3L} (one more layer of detail than
\code{\link[=fill_noise]{fill_noise()}}'s own default of \code{2L}).}

\item{seed}{Integer seed for the noise field, as in \code{\link[=shape_blob]{shape_blob()}}. Default
\code{1L}.}

\item{extend}{Passed to \code{\link[grid:pattern]{grid::pattern()}}. Default \code{"repeat"}.}
}
\value{
A pattern object as returned by \code{\link[grid:pattern]{grid::pattern()}}, suitable for
use as the \code{fill} argument to \code{\link[grid:gpar]{grid::gpar()}}.
}
\description{
\code{fill_charcoal()} is a \code{\link[=fill_noise]{fill_noise()}} preset -- same field, same
rendering, just different defaults -- tuned to read as hand-drawn
charcoal or marker grain rather than \code{\link[=fill_noise]{fill_noise()}}'s more
general-purpose rasterized field: a lighter base tone, finer/denser
tiling, and finer noise detail.
}
\details{
Found, while prototyping \code{\link[=shape_stroke]{shape_stroke()}}'s interior texture, to be a
substantially better fit than \code{\link[=fill_scribble]{fill_scribble()}} for a curved stroke's
body -- \code{fill_scribble()}'s fixed horizontal/vertical direction doesn't
track a curved path's own tangent (see the "Deferred: arbitrary angle
for \code{fill_scribble()}" item in \code{.agents/PLAN.md}), producing hatching
that visibly cuts across the stroke at odd angles wherever the path
bends, while \code{\link[=fill_noise]{fill_noise()}}'s directionless mottling has no orientation
to clash with the curve.
}
\examples{
draw(shape_stroke(
  x = c(0, 1, 2, 3), y = c(0, 1, 0, 1), width = 0.4,
  fill = fill_charcoal(), color = NA_character_
))

# a lighter tone and a curved backbone -- fill_charcoal()'s directionless
# mottling tracks a bend that fill_scribble()'s fixed direction wouldn't
t <- seq(0, 2 * pi, length.out = 200)
draw(shape_stroke(
  x = t, y = sin(t), width = 0.3,
  fill = fill_charcoal(color = "gray40"), color = NA_character_
))

}
\seealso{
Other fill helpers:
\code{\link[=fill_checker]{fill_checker()}},
\code{\link[=fill_crosshatch]{fill_crosshatch()}},
\code{\link[=fill_flow]{fill_flow()}},
\code{\link[=fill_gradient]{fill_gradient()}},
\code{\link[=fill_halftone]{fill_halftone()}},
\code{\link[=fill_hatch]{fill_hatch()}},
\code{\link[=fill_image]{fill_image()}},
\code{\link[=fill_marble]{fill_marble()}},
\code{\link[=fill_noise]{fill_noise()}},
\code{\link[=fill_none]{fill_none()}},
\code{\link[=fill_scatter]{fill_scatter()}},
\code{\link[=fill_scribble]{fill_scribble()}},
\code{\link[=fill_solid]{fill_solid()}},
\code{\link[=fill_stipple]{fill_stipple()}},
\code{\link[=fill_stripe]{fill_stripe()}},
\code{\link[=fill_vignette]{fill_vignette()}}
}
\concept{fill helpers}
