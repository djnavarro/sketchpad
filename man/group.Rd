% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group.R
\name{group}
\alias{group}
\title{A nested collection of drawables sharing one transform/style}
\usage{
group(shapes = list(), trans = trans_identity(), style = NULL)
}
\arguments{
\item{shapes}{A list of \link{drawable}/\code{group} objects. Default \code{list()}.}

\item{trans}{A \link{trans}/\link{trans_warp}/\link{trans_fn}/\link{trans_chain}, applied to
every member as a unit. Default \code{\link[=trans_identity]{trans_identity()}}.}

\item{style}{A \link{style} object overriding every descendant drawable's own
style, or \code{NULL} (default) for no override.}
}
\description{
\code{group} bundles several \link{drawable}/\code{group} objects (\code{shapes}) together
with one shared \code{trans} and, optionally, one shared \code{style} override --
the whole group can be rotated/scaled/moved as a unit, or restyled as a
unit, without touching any member's own \code{trans}/\code{style}. This is
different from \link{sketch}, which represents the whole canvas of
independently-styled/-positioned shapes; a \code{group} is meant to be one
element \emph{inside} a \link{sketch} (or inside another \code{group}), not a
replacement for it.
}
\details{
Groups are built up incrementally with \code{+}, the same way a \link{sketch} is:
\code{group() + shape_circle() + shape_circle(x = 2)}. \verb{group + <trans-like>}
composes the transform onto the group's own \verb{@trans}, applied to every
member as a unit -- it does not touch any member's own \verb{@trans}. A
\code{group} can itself be added to a \link{sketch} (or nested inside another
\code{group}), and mixed freely with plain \link{drawable}s in either container.

\code{style}, when set (default \code{NULL}, meaning no override), is meant to
replace every descendant \link{drawable}'s own \verb{@style} when the group is
drawn -- a nested \code{group} with no \code{style} of its own would inherit its
ancestor's override; a nested \code{group} that sets its own \code{style} would
keep that instead (the nearest override wins, it does not stack). This
cascade is implemented by \code{draw()}'s own \code{group} method, not by \code{group}
itself.
}
\examples{
# shape_square() (rather than shape_circle()) makes a rotation visually
# detectable -- a circle's outline is rotationally symmetric about its
# own centroid, so rotating one in place looks identical to the original
g <- group() + shape_square(side = 1) + shape_square(x = 2, side = 0.6)
draw(g)

# a trans applied to a group composes onto every member as a unit,
# without changing any member's own @trans -- both squares rotate about
# the group's own origin, sweeping the smaller one around the larger one
draw(g + trans_rotate(pi / 6))

# a style override, applied to every member when the group is drawn
draw(g + style(color = "tomato", fill = "grey90"))

# groups mix freely with plain drawables inside a sketch, and can nest;
# a nested group's own style override wins over an outer one, and an
# outer group's trans applies on top of everything inside it
inner <- group() + shape_square(x = 0.4, side = 0.3) + style(fill = "steelblue")
outer <- (group() + g + inner + style(color = "tomato")) + trans_translate(1, 0)
draw(sketch() + shape_square(x = -2) + outer)

}
\seealso{
Other core structure:
\code{\link[=canvas]{canvas()}},
\code{\link[=draw]{draw()}},
\code{\link[=drawable]{drawable()}},
\code{\link[=sketch]{sketch()}},
\code{\link[=style]{style()}},
\code{\link[=xy]{xy()}}
}
\concept{core structure}
