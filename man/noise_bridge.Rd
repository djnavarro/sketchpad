% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/noise_bridge.R
\name{noise_bridge}
\alias{noise_bridge}
\title{A smoothed Brownian bridge, as a path distortion}
\usage{
noise_bridge(smooth = 3L, seed = 1L)
}
\arguments{
\item{smooth}{Number of smoothing passes. Must be non-negative.
Default \code{3L}.}

\item{seed}{Integer seed. Default \code{1L}.}
}
\description{
\code{noise_bridge} bundles the settings needed to generate a smoothed
Brownian bridge (via the internal \code{smooth_bridge()} helper): how many
local-averaging \code{smooth}-ing passes to apply, and which \code{seed} to draw
from.
}
\details{
Unlike \link{noise_field} (sampled at arbitrary \verb{(x, y)} positions in
the plane), a Brownian bridge has no spatial position to sample at --
\code{\link[=noise_sample]{noise_sample()}}'s method for \code{noise_bridge} instead takes a point
count \code{n} and a \code{scale}, returning a length-\code{n} displacement vector.
This is the distortion behind \code{\link[=shape_twist]{shape_twist()}}'s wandering path, split
out into its own class for the same reason \link{noise_field} was: so the
distortion is a first-class, swappable object rather than bare
constructor arguments, and so a future \code{curve_twist()} (an open,
unfilled wandering path) can reuse it without duplicating
\code{shape_twist()}'s path logic.
}
\examples{
noise_bridge(smooth = 5L, seed = 4821L)

# more smoothing passes give a gentler bridge; embedding it in
# shape_twist()'s path_distortion makes the effect easy to see
draw(shape_twist(
  x = 0, y = 0, xend = 1, yend = 0, width = 0.2,
  path_distortion = noise_bridge(smooth = 0L, seed = 7734L)
))
draw(shape_twist(
  x = 0, y = 0, xend = 1, yend = 0, width = 0.2,
  path_distortion = noise_bridge(smooth = 20L, seed = 7734L)
))

}
\seealso{
Other noise helpers:
\code{\link[=noise_field]{noise_field()}},
\code{\link[=noise_sample]{noise_sample()}}
}
\concept{noise helpers}
