% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/points_raw.R
\name{points_raw}
\alias{points_raw}
\alias{points_raws}
\title{A scatter of points defined directly by their coordinates}
\usage{
points_raw(x, y, trans = trans_identity(), ...)

points_raws(x, y, trans = trans_identity(), ...)
}
\arguments{
\item{x, y}{For \code{points_raw()}, a numeric vector of x/y coordinates. For
\code{points_raws()}, a \code{list()} of such vectors instead -- one vector
per scatter.}

\item{trans}{A \link{trans} object applied to the computed points. Default
\code{\link[=trans_identity]{trans_identity()}} (no transform).}

\item{...}{Arguments passed to \code{\link[=style]{style()}}.}
}
\value{
A \link{drawable}.

For \code{points_raws()}, a \link{sketch}.
}
\description{
\code{points_raw} is \link{shape_raw}'s \code{"points"}-geometry analog, and the first
concrete constructor to use \code{geometry = "points"} (previously reserved
on the dimensional reading \code{"points"}(0D)/\code{"path"}(1D)/\code{"polygon"}(2D),
but with no constructor exposing it -- see \code{.agents/PLAN.md}). The user
supplies \code{x}/\code{y} coordinates directly, rendered as unconnected markers
rather than a connected outline or path.

\code{points_raws()} is a vectorized version of \code{\link[=points_raw]{points_raw()}}. Since
\code{x}/\code{y} are themselves numeric vectors of point coordinates for a
single scatter, \code{points_raws()} takes them as a \code{list()} of numeric
vectors instead -- one vector per scatter -- which is most useful for
giving several distinct scatters different \code{style} arguments (e.g. a
different \code{color} each).
}
\details{
\code{style@fill} and every line-related \code{style} property (\code{linewidth},
\code{linetype}, \code{linejoin}, \code{lineend}, \code{linemitre}) have no effect for
\code{points_raw()} -- see \link{drawable}'s \code{geometry} documentation and
\code{geometry_grob()}'s internal dispatch (\code{R/draw.R}) for why a \code{"points"}
geometry has no line to stroke and no interior to fill. Only \code{style@color}
is used, as the marker colour.

Every other argument may be a plain vector, recycled against \code{x}/\code{y}
via \code{purrr::pmap()}'s own vctrs-based rules (any length-1 element is
broadcast to the common length; mismatched lengths greater than 1
raise an error). The result is a \link{sketch} containing one
\code{points_raw()} per list element/recycled row, rather than a single
drawable.
}
\examples{
draw(points_raw(
  x = seq(0, 1, length.out = 20),
  y = sin(seq(0, 2 * pi, length.out = 20)) / 2 + 0.5,
  color = "steelblue"
))

# a random scatter, and the same points converted from a polygon's
# own outline (only style-related properties survive the round trip)
draw(points_raw(x = runif(200), y = runif(200)))
draw(S7::convert(
  shape_blob(radius = 1, distortion = noise_field(seed = 42L)),
  points_raw
))

# points_raw() is pathlike, so effect_tremor() can wobble the scatter
draw(effect_tremor(
  points_raw(x = seq(0, 1, length.out = 30), y = rep(0.5, 30)),
  layers = 6L, jitter = 0.08
))

draw(points_raws(
  x = list(seq(0, 1, length.out = 10), seq(0, 1, length.out = 10)),
  y = list(rep(0.25, 10), rep(0.75, 10)),
  color = c("steelblue", "darkred")
))

}
\concept{0D points}
