% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.R
\name{fill_halftone}
\alias{fill_halftone}
\title{Halftone dot pattern fill}
\usage{
fill_halftone(
  radius = c(0.05, 0.2),
  spacing = 0.3,
  aspect = 1,
  n = 4L,
  seed = 1L,
  color = "black",
  extend = "repeat"
)
}
\arguments{
\item{radius}{Dot radius range, as a length-2 numeric vector giving the
\code{"npc"}-fraction-of-tile minimum and maximum (a dot's actual radius is
drawn uniformly from this range). Both values must be positive, and
the first must be no larger than the second. Default \code{c(0.05, 0.2)}.}

\item{spacing}{Baseline tile size, as a fraction of the target's bounding
box. Must be a positive number. Default \code{0.3}.}

\item{aspect}{Width-to-height ratio of the target polygon's bounding box.
Must be a positive number. Default \code{1} (a square bounding box).}

\item{n}{Number of dots scattered per tile. Must be a positive integer.
Default \code{4L}.}

\item{seed}{Integer seed for the dot positions. Default \code{1L}.}

\item{color}{Dot colour. Default \code{"black"}.}

\item{extend}{Passed to \code{\link[grid:pattern]{grid::pattern()}}. Default \code{"repeat"}.}
}
\value{
A pattern object as returned by \code{\link[grid:pattern]{grid::pattern()}}, suitable for
use as the \code{fill} argument to \code{\link[grid:gpar]{grid::gpar()}}.
}
\description{
\code{fill_halftone()} is \code{\link[=fill_stipple]{fill_stipple()}}'s other variant: instead of
scattering dots of one fixed size, each dot's radius is drawn
uniformly at random from \code{radius}, giving a mottled halftone-print
look rather than a uniform stipple. Like \code{\link[=fill_stipple]{fill_stipple()}} (and unlike
\code{\link[=fill_scatter]{fill_scatter()}}), it scatters plain \code{\link[grid:circleGrob]{grid::circleGrob()}}s, so it's
immune to the \emph{polygon}-specific rendering problems documented at
\code{\link[=fill_scatter]{fill_scatter()}} -- but it shares \code{\link[=fill_stipple]{fill_stipple()}}'s own, separate
"Known rendering risk with multiple dots" (repeated tiles containing
several \code{circleGrob}s were, in testing, sometimes visibly distorted on
this package's development R build; see that section for details).
There is no known way to avoid this while still getting a genuine
scattered-dot texture, so \strong{check rendered output visually} here too.
}
\details{
As with \code{\link[=fill_stipple]{fill_stipple()}}, the target's bounding-box aspect ratio needs
to be passed as \code{aspect} to keep the dots circular rather than
elliptical; the default \code{aspect = 1} is only exact for a square
bounding box. Dot centres are kept at least \code{max(radius)} from each
tile edge, so even the largest possible dot isn't clipped away near a
boundary.
}
\seealso{
Other fill helpers:
\code{\link[=fill_checker]{fill_checker()}},
\code{\link[=fill_crosshatch]{fill_crosshatch()}},
\code{\link[=fill_flow]{fill_flow()}},
\code{\link[=fill_gradient]{fill_gradient()}},
\code{\link[=fill_hatch]{fill_hatch()}},
\code{\link[=fill_image]{fill_image()}},
\code{\link[=fill_marble]{fill_marble()}},
\code{\link[=fill_noise]{fill_noise()}},
\code{\link[=fill_scatter]{fill_scatter()}},
\code{\link[=fill_scribble]{fill_scribble()}},
\code{\link[=fill_solid]{fill_solid()}},
\code{\link[=fill_stipple]{fill_stipple()}},
\code{\link[=fill_stripe]{fill_stripe()}},
\code{\link[=fill_vignette]{fill_vignette()}}
}
\concept{fill helpers}
