% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette.R
\name{palette_cosine}
\alias{palette_cosine}
\title{Colour palette from a linear cosine formula}
\usage{
palette_cosine(n, base = NULL, seed = 1L)
}
\arguments{
\item{n}{A single positive whole number: the number of colours to
generate.}

\item{base}{\code{NULL} (the default) or a character vector of candidate
colours to sample \code{b}, \code{c}, and \code{d} from. \code{NULL} uses
\code{grDevices::colors(distinct = TRUE)}.}

\item{seed}{A single whole number seeding the random sampling of \code{b},
\code{c}, and \code{d}, so the same \code{seed} always produces the same palette.}
}
\value{
A character vector of \code{n} hex colour strings.
}
\description{
Generates a smoothly varying colour palette using the linear cosine
formula described by Iñigo Quilez for procedural palettes (see
\url{https://blog.djnavarro.net/posts/2025-09-14_cosine-palettes/}):
\deqn{f(t) = a + b \cos(2 \pi (c t + d))}
where \eqn{a = (0.5, 0.5, 0.5)} is fixed and \eqn{b}, \eqn{c}, \eqn{d}
are each an RGB triple sampled (with replacement) from \code{base}.
}
\details{
As in the source algorithm, resulting values are clamped only on the
high end (values above 1 are capped at 1); values below 0 are folded
back with \code{\link[=abs]{abs()}} rather than clamped to 0. This is an intentional
quirk inherited from the source algorithm, not a bug -- it occasionally
produces a colour "reflected" around black rather than a flat black.
}
\examples{
palette_cosine(n = 16, seed = 11)

}
\seealso{
Other palette helpers:
\code{\link[=palette_manual]{palette_manual()}}
}
\concept{palette helpers}
