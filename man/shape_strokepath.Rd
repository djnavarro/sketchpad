% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_strokepath.R
\name{shape_strokepath}
\alias{shape_strokepath}
\alias{shape_strokepaths}
\title{A ribbon following an arbitrary curve}
\usage{
shape_strokepath(
  path,
  width = 0.2,
  n = 100L,
  distortion = noise_field(),
  trans = trans_identity(),
  ...
)

shape_strokepaths(
  path,
  width = 0.2,
  n = 100L,
  distortion = noise_field(),
  trans = trans_identity(),
  ...
)
}
\arguments{
\item{path}{A \link{drawable} with \code{geometry == "path"} (i.e. any
\verb{curve_*()} constructor's output, e.g. \code{\link[=curve_bezier]{curve_bezier()}},
\code{\link[=curve_line]{curve_line()}}, \code{\link[=curve_twist]{curve_twist()}}, \code{\link[=curve_spiral]{curve_spiral()}}, \code{\link[=curve_arc]{curve_arc()}},
\code{\link[=curve_scribble]{curve_scribble()}}, \code{\link[=curve_raw]{curve_raw()}}) -- its own computed points become
this ribbon's backbone.}

\item{width}{Maximum width. Must be non-negative. Default \code{0.2}.}

\item{n}{Number of points used along the ribbon, resampled evenly by
arc length from \code{path}'s own points (independent of however many
points \code{path} itself was sampled at). Must be at least \code{2}. Default
\code{100L}.}

\item{distortion}{A \link{noise_field} controlling the width ("pressure")
modulation. Default \code{noise_field()}.}

\item{trans}{A \link{trans} object applied to the ribbon's computed points,
on top of any \code{trans} already applied to \code{path} itself. Default
\code{\link[=trans_identity]{trans_identity()}} (no transform).}

\item{...}{Arguments passed to \code{\link[=style]{style()}}.}
}
\value{
A \link{drawable} (a \link{shape_stroke}).

For \code{shape_strokepaths()}, a \link{sketch}.
}
\description{
\code{shape_strokepath()} builds a tapered, noise-modulated ribbon (like
\code{\link[=shape_stroke]{shape_stroke()}}) whose backbone follows an arbitrary \verb{curve_*()}
drawable's own computed points, rather than raw \code{x}/\code{y} control
points.

\code{shape_strokepaths()} is a vectorized version of
\code{\link[=shape_strokepath]{shape_strokepath()}}: each argument may be a vector, recycled against
the others via \code{purrr::pmap()}'s own vctrs-based rules. The
result is a \link{sketch} containing one \code{shape_strokepath()} per recycled
row, rather than a single drawable.
}
\details{
It is a thin wrapper: \code{path@points} is extracted and fed
straight into \code{\link[=shape_stroke]{shape_stroke()}}, so the object it returns is literally
a \code{shape_stroke} -- \code{shape_strokepath()} exists only to save the
caller from writing \code{shape_stroke(x = path@points@x, y = path@points@y, ...)} by hand, and to make the "ribbon around a curve"
use case discoverable under its own name.

Because the result is a \code{shape_stroke}, its width offset uses a true
per-point unit normal (\code{shape_stroke()}'s own \code{stroke_normals()}
helper) rather than a single shared offset direction -- this is what
lets \code{shape_strokepath()} work correctly for any backbone shape,
including ones that loop or bend sharply (e.g. \code{\link[=curve_twist]{curve_twist()}},
\code{\link[=curve_spiral]{curve_spiral()}}), not just a nearly-straight one. Passing a
\code{\link[=curve_bezier]{curve_bezier()}} here covers the same use case the package's earlier
\code{shape_bezier_ribbon()} (since removed) provided, but the two are not
numerically identical for a curved backbone: \code{shape_bezier_ribbon()}
offset by one shared direction vector (the straight line from its
start to end point) for the whole ribbon, and its taper formula
peaked at \code{0.5}; \code{shape_strokepath()} instead inherits
\code{shape_stroke()}'s true per-point normals and its taper formula
(which peaks at \code{1}, so \code{width} is exactly the maximum rendered
width).

Any length-1 element is broadcast to the common length; mismatched
lengths greater than 1 raise an error. Unlike the \code{x}/\code{y}-list-column
constructors (e.g. \code{\link[=shape_beziers]{shape_beziers()}}), \code{path} is a single \link{drawable}
object per ribbon, not a numeric vector, so a single shared \code{path}
recycles automatically across every ribbon (the same way a shared
\code{distortion} \link{noise_field} already does); pass a \code{list()} of several
different \verb{curve_*()} objects instead to vary the backbone per ribbon.
}
\examples{
draw(shape_strokepath(
  curve_bezier(x = c(0, 0.25, 0.75, 1), y = c(0, 1, -1, 0)),
  width = 0.2
))
draw(shape_strokepath(
  curve_twist(
    x = 0,
    y = 0,
    xend = 1,
    yend = 0,
    path_distortion = noise_bridge(seed = 7734)
  ),
  width = 0.15
))

# a ribbon around a spiral -- a backbone shape_ribbon()/shape_twist()'s
# shared single offset direction couldn't render correctly
draw(shape_strokepath(
  curve_spiral(radius_start = 0.1, radius_end = 1, turns = 3),
  width = 0.1, fill = fill_charcoal()
))

draw(shape_strokepaths(
  path = list(
    curve_bezier(x = c(0, 0.25, 0.75, 1), y = c(0, 1, -1, 0)),
    curve_line(x = c(0, 1, 2), y = c(2, 3, 2))
  ),
  width = 0.2
))

}
\seealso{
Other 2D shapes:
\code{\link[=shape_bezier]{shape_bezier()}},
\code{\link[=shape_blob]{shape_blob()}},
\code{\link[=shape_circle]{shape_circle()}},
\code{\link[=shape_combine]{shape_combine()}},
\code{\link[=shape_ellipse]{shape_ellipse()}},
\code{\link[=shape_polygon]{shape_polygon()}},
\code{\link[=shape_raw]{shape_raw()}},
\code{\link[=shape_rectangle]{shape_rectangle()}},
\code{\link[=shape_ribbon]{shape_ribbon()}},
\code{\link[=shape_star]{shape_star()}},
\code{\link[=shape_stroke]{shape_stroke()}},
\code{\link[=shape_twist]{shape_twist()}},
\code{\link[=shape_wedge]{shape_wedge()}}
}
\concept{2D shapes}
