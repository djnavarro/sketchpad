% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trans.R
\name{trans}
\alias{trans}
\title{A 2D affine transformation}
\usage{
trans(matrix = integer(0))
}
\arguments{
\item{matrix}{A 3x3 numeric matrix in homogeneous coordinates (i.e. its
third row must be \code{c(0, 0, 1)}).}
}
\description{
\code{trans} wraps a 3x3 homogeneous-coordinates affine transformation
matrix. It is not usually constructed directly -- use \code{\link[=trans_identity]{trans_identity()}},
\code{\link[=trans_translate]{trans_translate()}}, \code{\link[=trans_rotate]{trans_rotate()}}, \code{\link[=trans_scale]{trans_scale()}},
\code{\link[=trans_reflect]{trans_reflect()}}, \code{\link[=trans_shear]{trans_shear()}}, or the general-purpose
\code{\link[=trans_affine]{trans_affine()}} instead. Every \link{drawable} carries a \code{trans} property
(default \code{\link[=trans_identity]{trans_identity()}}) that's applied to its computed \code{points} as
the very last step -- see \link{drawable}'s \code{trans} documentation.
}
\details{
Two \code{trans} objects combine with \code{+}: \code{t1 + t2} produces a new \code{trans}
whose effect is "apply \code{t1} first, then \code{t2}" -- see \code{\link[=trans_translate]{trans_translate()}}'s
Details for the composition order convention and a worked example.
Composing a \code{trans} with a \link{trans_warp} (a non-rigid, noise-based
deformation that can't be represented as a matrix) instead produces a
\link{trans_chain} -- see there.
}
\examples{
trans_identity()
trans_translate(1, 0) + trans_rotate(pi / 4)

# overlay a shape's original outline (faded) with a transformed copy
# (solid) to see a trans's effect directly
original <- shape_rectangle(width = 1.5, height = 0.6, fill_alpha = 0.3, color_alpha = 0.3)
draw(sketch() + original + (original + trans_rotate(pi / 6)))

}
\seealso{
Other transform helpers:
\code{\link[=trans_affine]{trans_affine()}},
\code{\link[=trans_chain]{trans_chain()}},
\code{\link[=trans_identity]{trans_identity()}},
\code{\link[=trans_reflect]{trans_reflect()}},
\code{\link[=trans_rotate]{trans_rotate()}},
\code{\link[=trans_scale]{trans_scale()}},
\code{\link[=trans_shear]{trans_shear()}},
\code{\link[=trans_translate]{trans_translate()}},
\code{\link[=trans_warp]{trans_warp()}}
}
\concept{transform helpers}
