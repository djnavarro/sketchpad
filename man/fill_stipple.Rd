% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.R
\name{fill_stipple}
\alias{fill_stipple}
\title{Stippled dot pattern fill}
\usage{
fill_stipple(
  radius = 0.15,
  spacing = 0.3,
  aspect = 1,
  n = 4L,
  seed = 1L,
  color = "black",
  extend = "repeat"
)
}
\arguments{
\item{radius}{Dot radius, as a \code{"npc"} fraction of the tile. Must be a
positive number. Default \code{0.15}.}

\item{spacing}{Baseline tile size, as a fraction of the target's bounding
box. Must be a positive number. Default \code{0.1}.}

\item{aspect}{Width-to-height ratio of the target polygon's bounding box.
Must be a positive number. Default \code{1} (a square bounding box).}

\item{n}{Number of dots scattered per tile. Must be a positive integer.
Default \code{4L}.}

\item{seed}{Integer seed for the dot positions. Default \code{1L}.}

\item{color}{Line colour. Default \code{"black"}.}

\item{extend}{Passed to \code{\link[grid:pattern]{grid::pattern()}}. Default \code{"repeat"}.}
}
\value{
A pattern object as returned by \code{\link[grid:pattern]{grid::pattern()}}, suitable for
use as the \code{fill} argument to \code{\link[grid:gpar]{grid::gpar()}}.
}
\description{
\code{fill_stipple()} builds a \code{\link[grid:pattern]{grid::pattern()}} fill value that scatters a
handful of dots at random positions inside each tile, using
\code{\link[withr:with_seed]{withr::with_seed()}} so the same \code{seed} always reproduces the same
scatter (the same convention used by \code{\link[=blob]{blob()}}, \code{\link[=ribbon]{ribbon()}}, and
\code{\link[=twist]{twist()}}'s noise fields).
}
\details{
Unlike \code{\link[=fill_hatch]{fill_hatch()}}/\code{\link[=fill_crosshatch]{fill_crosshatch()}}, a dot has no direction, so
there's no analogue of their tile-edge "dashing" problem here. There's
still a circularity problem to correct for, though: \code{\link[grid:pattern]{grid::pattern()}}
tiles are sized as a fraction of the target polygon's own bounding box,
so a dot drawn with an \code{npc}-relative radius renders as an ellipse
whenever that bounding box isn't square. Pass the bounding box's
width-to-height ratio as \code{aspect} to keep dots circular; the default
\code{aspect = 1} is only exact for a square bounding box.
}
\seealso{
Other fill helpers:
\code{\link[=fill_checker]{fill_checker()}},
\code{\link[=fill_crosshatch]{fill_crosshatch()}},
\code{\link[=fill_gradient]{fill_gradient()}},
\code{\link[=fill_hatch]{fill_hatch()}},
\code{\link[=fill_noise]{fill_noise()}},
\code{\link[=fill_solid]{fill_solid()}},
\code{\link[=fill_stripe]{fill_stripe()}},
\code{\link[=fill_vignette]{fill_vignette()}}
}
\concept{fill helpers}
