% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.R
\name{fill_stipple}
\alias{fill_stipple}
\title{Stippled dot pattern fill}
\usage{
fill_stipple(
  color = "black",
  radius = 0.15,
  spacing = 0.3,
  aspect = 1,
  n = 4L,
  seed = 1L,
  extend = "repeat"
)
}
\arguments{
\item{color}{One or more dot colours. A vector shorter than \code{n} is
recycled (in order, not randomly) across the scattered dots -- a single
colour (the default) colours every dot the same, matching the original
behaviour. Default \code{"black"}.}

\item{radius}{Dot radius, as a \code{"npc"} fraction of the tile. Must be a
positive number. Default \code{0.15}.}

\item{spacing}{Baseline tile size, as a fraction of the target's bounding
box. Must be a positive number. Default \code{0.3}.}

\item{aspect}{Width-to-height ratio of the target polygon's bounding box.
Must be a positive number. Default \code{1} (a square bounding box).}

\item{n}{Number of dots scattered per tile. Must be a positive integer.
Default \code{4L}.}

\item{seed}{Integer seed for the dot positions. Default \code{1L}.}

\item{extend}{Passed to \code{\link[grid:pattern]{grid::pattern()}}. Default \code{"repeat"}.}
}
\value{
A pattern object as returned by \code{\link[grid:pattern]{grid::pattern()}}, suitable for
use as the \code{fill} argument to \code{\link[grid:gpar]{grid::gpar()}}.
}
\description{
\code{fill_stipple()} builds a \code{\link[grid:pattern]{grid::pattern()}} fill value that scatters a
handful of dots at random positions inside each tile, using
\code{\link[withr:with_seed]{withr::with_seed()}} so the same \code{seed} always reproduces the same
scatter (the same convention used by \code{\link[=shape_blob]{shape_blob()}}, \code{\link[=shape_ribbon]{shape_ribbon()}},
and \code{\link[=shape_twist]{shape_twist()}}'s noise fields).
}
\details{
Unlike \code{\link[=fill_hatch]{fill_hatch()}}/\code{\link[=fill_crosshatch]{fill_crosshatch()}}, a dot has no direction, so
there's no analogue of their tile-edge "dashing" problem here. There's
still a circularity problem to correct for, though: \code{\link[grid:pattern]{grid::pattern()}}
tiles are sized as a fraction of the target polygon's own bounding box,
so a dot drawn with an \code{npc}-relative radius renders as an ellipse
whenever that bounding box isn't square. Pass the bounding box's
width-to-height ratio as \code{aspect} to keep dots circular; the default
\code{aspect = 1} is only exact for a square bounding box.
}
\section{Known rendering risk with multiple dots}{
 On this package's
development R build (4.6.1, a very recent/development version),
\code{\link[grid:pattern]{grid::pattern()}} tiles whose content is a \emph{group} of several
\code{\link[grid:circleGrob]{grid::circleGrob()}}s (i.e. \code{n > 1}) were found, in some cases, to
render individual dots visibly distorted -- clipped into crescents or
otherwise not circular -- even though each dot's own coordinates are
correct and a single dot (\code{n = 1}) always renders correctly. This
reproduced in a fresh R session (so it isn't specific to a long
interactive session), across multiple \code{n} and \code{radius} values, with
no clean rule found for exactly when it triggers; it appeared on both
an interactive device and \code{ragg::agg_png()}. No fix or reliable
workaround was found -- this looks like an upstream \code{grid}/Cairo
issue with multi-shape pattern tile content, not something specific
to how this function builds its content. \strong{Visually check rendered
output} before relying on \code{fill_stipple()} (or \code{\link[=fill_scatter]{fill_scatter()}}/
\code{\link[=fill_halftone]{fill_halftone()}}, which share this risk) for anything beyond casual
use, especially on unfamiliar R/\code{grid}/graphics-device versions.
}

\examples{
draw(shape_circle(fill = fill_stipple(n = 6L, seed = 2091L)))

# more, smaller dots per tile give a denser stipple
draw(shape_circle(
  fill = fill_stipple(n = 15L, radius = 0.06, spacing = 0.5, seed = 2091L)
))

# a colour vector is recycled across the dots
draw(shape_circle(
  fill = fill_stipple(color = c("steelblue", "tomato"), n = 8L, seed = 2091L)
))

}
\seealso{
Other fill helpers:
\code{\link[=fill_charcoal]{fill_charcoal()}},
\code{\link[=fill_checker]{fill_checker()}},
\code{\link[=fill_crosshatch]{fill_crosshatch()}},
\code{\link[=fill_flow]{fill_flow()}},
\code{\link[=fill_gradient]{fill_gradient()}},
\code{\link[=fill_halftone]{fill_halftone()}},
\code{\link[=fill_hatch]{fill_hatch()}},
\code{\link[=fill_image]{fill_image()}},
\code{\link[=fill_marble]{fill_marble()}},
\code{\link[=fill_noise]{fill_noise()}},
\code{\link[=fill_none]{fill_none()}},
\code{\link[=fill_scatter]{fill_scatter()}},
\code{\link[=fill_scribble]{fill_scribble()}},
\code{\link[=fill_solid]{fill_solid()}},
\code{\link[=fill_stripe]{fill_stripe()}},
\code{\link[=fill_vignette]{fill_vignette()}}
}
\concept{fill helpers}
