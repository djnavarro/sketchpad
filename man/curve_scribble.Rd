% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curve_scribble.R
\name{curve_scribble}
\alias{curve_scribble}
\alias{curve_scribbles}
\title{A wandering scribble curve}
\usage{
curve_scribble(
  x = 0,
  y = 0,
  width = 1,
  height = 1,
  direction = "horizontal",
  n_harmonics = 3L,
  amplitude = 0.35,
  n = 200L,
  seed = 1L,
  trans = trans_identity(),
  ...
)

curve_scribbles(
  x = 0,
  y = 0,
  width = 1,
  height = 1,
  direction = "horizontal",
  n_harmonics = 3L,
  amplitude = 0.35,
  n = 200L,
  seed = 1L,
  trans = trans_identity(),
  ...
)
}
\arguments{
\item{x, y}{Coordinates of the curve's origin. Default \code{0}.}

\item{width, height}{Extent of the curve's bounding box along/across its
direction of travel. Must be positive. Default \code{1}.}

\item{direction}{Either \code{"horizontal"} (the curve runs left-right) or
\code{"vertical"} (top-bottom). Default \code{"horizontal"}.}

\item{n_harmonics}{Number of sine harmonics summed to build the curve.
Must be a positive integer. Default \code{3L}.}

\item{amplitude}{Maximum total wiggle amplitude, as a fraction of
\code{height} (for \code{direction = "horizontal"}) or \code{width} (for
\code{"vertical"}), split across \code{n_harmonics}. Must be a non-negative
number. Default \code{0.35}.}

\item{n}{Number of points sampled along the curve. Must be a positive
integer of at least \code{2L}. Default \code{200L}.}

\item{seed}{Integer seed for the random harmonics. Default \code{1L}.}

\item{trans}{A \link{trans} object applied to the curve's computed points.
Default \code{\link[=trans_identity]{trans_identity()}} (no transform).}

\item{...}{Arguments passed to \code{\link[=style]{style()}}.}
}
\value{
A \link{drawable}.

For \code{curve_scribbles()}, a \link{sketch}.
}
\description{
\code{curve_scribble} is a \link{drawable} whose path is a single random wandering
line -- a random finite sum of sine harmonics -- built from the same
internal \code{scribble_lines()} generator that \code{\link[=fill_scribble]{fill_scribble()}} uses to
texture a tile's interior, but scaled here into an arbitrary bounding box
on the sketch's own coordinate plane rather than tiled inside a fill
pattern. Where \code{fill_scribble()} scatters several such lines across a
repeating tile as a texture, \code{curve_scribble()} draws exactly one as a
standalone open curve.

\code{curve_scribbles()} is a vectorized version of \code{\link[=curve_scribble]{curve_scribble()}}:
each argument may be a vector, recycled against the others via
\code{purrr::pmap()}'s own vctrs-based rules (any length-1 element is
broadcast to the common length; mismatched lengths greater than 1
raise an error). Varying \code{seed} per curve is usually what makes
several scribbles look distinct from each other. The result is a
\link{sketch} containing one \code{curve_scribble()} per recycled row, rather
than a single drawable.
}
\details{
The underlying line is generated in \verb{(along, across)} form, \code{along}
running from \code{0} to \code{1} and \code{across} wandering around a random baseline
near \code{0.5} (see \code{scribble_lines()}'s own details for why this
particular construction was chosen -- periodicity, needed for tiling,
is irrelevant here). \code{x}/\code{y} place the curve's origin, and \code{width}/
\code{height} scale it: for \code{direction = "horizontal"} (the default), \code{along}
maps to \code{x + along * width} and \code{across} to \code{y + across * height}, so
the curve runs left-to-right; for \code{direction = "vertical"}, the mapping
swaps (\code{along} maps to \code{y}, \code{across} to \code{x}), so the curve runs
bottom-to-top instead.

\code{style@fill} has no effect for \code{curve_scribble()} -- see \link{drawable}'s
\code{geometry} documentation for why \code{"path"} geometries have no interior to
fill. Passing \code{fill} via \code{...} is still accepted (it's simply ignored at
draw time), since \code{style()} is shared across every \code{geometry}.
}
\examples{
draw(curve_scribble(width = 2, height = 0.5, seed = 5591L))

draw(curve_scribbles(x = 1:3, width = 0.8, height = 0.5, seed = 1:3))

}
\seealso{
Other 1D curves:
\code{\link[=curve_arc]{curve_arc()}},
\code{\link[=curve_bezier]{curve_bezier()}},
\code{\link[=curve_line]{curve_line()}},
\code{\link[=curve_raw]{curve_raw()}},
\code{\link[=curve_spiral]{curve_spiral()}},
\code{\link[=curve_twist]{curve_twist()}}
}
\concept{1D curves}
