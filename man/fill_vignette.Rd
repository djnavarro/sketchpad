% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.R
\name{fill_vignette}
\alias{fill_vignette}
\title{Vignette fill}
\usage{
fill_vignette(color = "black", background = NA, spacing = 1, aspect = 1)
}
\arguments{
\item{color}{Fill colour at the tile's centre. Default \code{"black"}.}

\item{background}{Fill colour revealed as \code{color} fades out, or \code{NA} for
true transparency (showing whatever is drawn behind the target shape).
Default \code{NA}.}

\item{spacing}{Tile size, as a fraction of the target's bounding box.
Must be a positive number. Default \code{1} (one tile spans the whole
shape).}

\item{aspect}{Width-to-height ratio of the target polygon's bounding box.
Must be a positive number. Default \code{1} (a square bounding box).}
}
\value{
A pattern object as returned by \code{\link[grid:pattern]{grid::pattern()}}, suitable for
use as the \code{fill} argument to \code{\link[grid:gpar]{grid::gpar()}}.
}
\description{
\code{fill_vignette()} builds a \code{\link[grid:pattern]{grid::pattern()}} fill value that fades a
colour out towards the edges of each tile, using \code{\link[grid:as.mask]{grid::as.mask()}} --
the one \code{grid} capability the rest of the \verb{fill_*()} family doesn't
touch. A solid \code{color} layer is masked by a radial alpha mask (opaque at
the tile centre, fully transparent at its edge), optionally revealing a
solid \code{background} layer underneath rather than true transparency.
}
\details{
As with \code{\link[=fill_gradient]{fill_gradient()}}, the fade shape is kept circular by correcting
the \emph{tile} to be physically square via \code{aspect} (the target's
bounding-box width-to-height ratio), the same technique \code{\link[=fill_stipple]{fill_stipple()}}
uses for its dots -- once the tile is square, a mask specified inside it
in plain \code{"npc"} needs no further correction.

A mask must always be built with \code{\link[grid:as.mask]{grid::as.mask()}} and an explicit
\code{type = "alpha"} here, rather than passed as a bare grob (which defaults
to an alpha mask anyway) -- during prototyping, a bare mask grob whose
own fill was a \code{\link[grid:radialGradient]{grid::radialGradient()}} intermittently triggered an
"Ignored luminance mask (not supported on this device)" warning on this
session's device, even though the rendered result was visually correct
either way. Being explicit with \code{as.mask(..., type = "alpha")} avoided
the warning entirely with an identical render, so that's what's used
here; true \code{\link[grid:as.mask]{grid::as.mask()}} luminance masks were found not to work at
all in this nested tile context (silently ignored, regardless of
explicitness), so \code{fill_vignette()} only offers the alpha variant.
}
\seealso{
Other fill helpers:
\code{\link[=fill_checker]{fill_checker()}},
\code{\link[=fill_crosshatch]{fill_crosshatch()}},
\code{\link[=fill_gradient]{fill_gradient()}},
\code{\link[=fill_halftone]{fill_halftone()}},
\code{\link[=fill_hatch]{fill_hatch()}},
\code{\link[=fill_noise]{fill_noise()}},
\code{\link[=fill_scatter]{fill_scatter()}},
\code{\link[=fill_solid]{fill_solid()}},
\code{\link[=fill_stipple]{fill_stipple()}},
\code{\link[=fill_stripe]{fill_stripe()}}
}
\concept{fill helpers}
