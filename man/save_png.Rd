% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{save_png}
\alias{save_png}
\alias{save_svg}
\alias{save_pdf}
\title{Save a drawable or sketch to an image file}
\usage{
save_png(
  object,
  filename,
  width = 7,
  height = 7,
  dpi = 300,
  bg = "white",
  units = "in",
  ...
)

save_svg(object, filename, width = 7, height = 7, bg = "white", ...)

save_pdf(object, filename, width = 7, height = 7, bg = "white", ...)
}
\arguments{
\item{object}{A \link{drawable} or \link{sketch} object.}

\item{filename}{A single string, the path to write to.}

\item{width, height}{Image dimensions, in \code{units} for \code{save_png()} (default
inches) or always in inches for \code{save_svg()}/\code{save_pdf()}, which have no
\code{units} argument of their own. Default \code{7}.}

\item{dpi}{Resolution in dots per inch. Only meaningful for
\code{save_png()} (a raster format); ignored by \code{save_svg()}/\code{save_pdf()},
which are drawn at vector resolution. Default \code{300}.}

\item{bg}{Background colour passed to the underlying device, e.g.
\code{"white"} (the default) or \code{"transparent"}. This is the device's own
page colour, independent of any \code{\link[=canvas]{canvas()}} \code{background} a \code{sketch}
itself already carries -- the two compose, so a transparent \code{bg}
here still shows an opaque \code{canvas()} background underneath, and vice
versa a \code{sketch} with no \code{canvas()} background shows \code{bg} through
any of its own shapes that don't fully cover the page.}

\item{units}{One of \code{"in"} (the default), \code{"cm"}, \code{"mm"}, or \code{"px"},
forwarded to \code{\link[grDevices:png]{grDevices::png()}}'s own \code{units} argument -- only
meaningful for \code{save_png()}; \code{save_svg()}/\code{save_pdf()} have no \code{units}
argument, since \code{\link[grDevices:svg]{grDevices::svg()}}/\code{\link[grDevices:pdf]{grDevices::pdf()}} always take
\code{width}/\code{height} in inches.}

\item{...}{Passed on to \code{\link[=draw]{draw()}}, e.g. \code{xlim}/\code{ylim}.}
}
\value{
\code{filename}, invisibly.
}
\description{
\code{save_png()}, \code{save_svg()}, and \code{save_pdf()} render a \link{drawable} or
\link{sketch} straight to a raster (PNG) or vector (SVG/PDF) file, without
the caller needing to manage a \link{grDevices} device by hand. Each is a
thin wrapper: open the appropriate device, call \code{\link[=draw]{draw()}}, and always
close the device afterward (via \code{on.exit()}, so a device is never left
open even if \code{draw()} itself errors).
}
\examples{
file <- tempfile(fileext = ".png")
save_png(shape_circle(radius = 1), file)

file <- tempfile(fileext = ".svg")
save_svg(shape_circle(radius = 1), file)

file <- tempfile(fileext = ".pdf")
save_pdf(shape_circle(radius = 1), file)

}
\concept{export helpers}
