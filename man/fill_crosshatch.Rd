% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.R
\name{fill_crosshatch}
\alias{fill_crosshatch}
\title{Crosshatch pattern fill}
\usage{
fill_crosshatch(
  angle = 45,
  spacing = 0.1,
  aspect = 1,
  color = "black",
  linewidth = 1,
  extend = "repeat"
)
}
\arguments{
\item{angle}{Hatch angle in degrees, measured counterclockwise from the
positive x-axis. Default \code{45}.}

\item{spacing}{Baseline tile size, as a fraction of the target's bounding
box. Must be a positive number. Default \code{0.1}.}

\item{aspect}{Width-to-height ratio of the target polygon's bounding box.
Must be a positive number. Default \code{1} (a square bounding box).}

\item{color}{Line colour. Default \code{"black"}.}

\item{linewidth}{Line width. Default \code{1}.}

\item{extend}{Passed to \code{\link[grid:pattern]{grid::pattern()}}. Default \code{"repeat"}.}
}
\value{
A pattern object as returned by \code{\link[grid:pattern]{grid::pattern()}}, suitable for
use as the \code{fill} argument to \code{\link[grid:gpar]{grid::gpar()}}.
}
\description{
\code{fill_crosshatch()} builds a \code{\link[grid:pattern]{grid::pattern()}} fill value that renders two
mirror-symmetric hatch lines, at \code{angle} and \code{-angle}, forming an "X"
inside each tile. It shares \code{\link[=fill_hatch]{fill_hatch()}}'s tile-shape technique: both
lines are drawn as the two corner-to-corner diagonals of a single
rectangular tile (rather than at an arbitrary baked-in slope), so both
tile seamlessly under \code{extend = "repeat"}, and the tile's \code{width}/\code{height}
ratio -- not the diagonals' own coordinates -- determines the rendered
angle. See \code{\link[=fill_hatch]{fill_hatch()}}'s details for why this matters.
}
\details{
Because both lines share one tile shape, they are only \emph{perpendicular}
when \code{angle = 45} (the classic crosshatch look); for other angles the two
lines are symmetric about the horizontal axis but not at right angles to
each other. Genuinely perpendicular hatching at an arbitrary angle would
need two differently-shaped tiles layered as separate fills, which this
function does not attempt.

At \code{angle} a multiple of 90 degrees, the two mirrored diagonals would
coincide, so this case is handled separately by drawing a horizontal line
and a vertical line instead (a simple grid).
}
\seealso{
Other fill helpers:
\code{\link[=fill_checker]{fill_checker()}},
\code{\link[=fill_gradient]{fill_gradient()}},
\code{\link[=fill_halftone]{fill_halftone()}},
\code{\link[=fill_hatch]{fill_hatch()}},
\code{\link[=fill_noise]{fill_noise()}},
\code{\link[=fill_scatter]{fill_scatter()}},
\code{\link[=fill_solid]{fill_solid()}},
\code{\link[=fill_stipple]{fill_stipple()}},
\code{\link[=fill_stripe]{fill_stripe()}},
\code{\link[=fill_vignette]{fill_vignette()}}
}
\concept{fill helpers}
