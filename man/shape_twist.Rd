% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_twist.R
\name{shape_twist}
\alias{shape_twist}
\alias{shape_twists}
\title{A twisted ribbon following a random path}
\usage{
shape_twist(
  x = 0,
  y = 0,
  xend = 1,
  yend = 1,
  width = 0.2,
  n = 100L,
  path_distortion = noise_bridge(),
  distortion = noise_field(),
  trans = trans_identity(),
  ...
)

shape_twists(
  x = 0,
  y = 0,
  xend = 1,
  yend = 1,
  width = 0.2,
  n = 100L,
  path_distortion = noise_bridge(),
  distortion = noise_field(),
  trans = trans_identity(),
  ...
)
}
\arguments{
\item{x, y}{Start point. Default \code{0}.}

\item{xend, yend}{End point. Default \code{1}.}

\item{width}{Maximum width. Must be non-negative. Default \code{0.2}.}

\item{n}{Number of points used along the path. Default \code{100L}.}

\item{path_distortion}{A \link{noise_bridge} controlling the path's
Brownian bridge. Default \code{noise_bridge()}.}

\item{distortion}{A \link{noise_field} controlling the width modulation.
Default \code{noise_field()}.}

\item{trans}{A \link{trans} object applied to the shape's computed points.
Default \code{\link[=trans_identity]{trans_identity()}} (no transform).}

\item{...}{Arguments passed to \code{\link[=style]{style()}}.}
}
\value{
A \link{drawable}.

For \code{shape_twists()}, a \link{sketch}.
}
\description{
\code{shape_twist} is like \link{shape_ribbon}, but the underlying path is a
Brownian bridge rather than a straight line, giving the polygon a
wandering, twisted appearance.

\code{shape_twists()} is a vectorized version of \code{\link[=shape_twist]{shape_twist()}}: each
argument may be a vector, recycled against the others via
\code{purrr::pmap()}'s own vctrs-based rules (any length-1 element is
broadcast to the common length; mismatched lengths greater than 1
raise an error). A shared \code{path_distortion}/\code{distortion} is
automatically recycled across every twist; pass a \code{list()} of several
different \link{noise_bridge}/\link{noise_field} objects instead to vary either
per twist -- as in \code{README.Rmd}'s "Twists" example, which gives every
twist the same \code{path_distortion} this way. The result is a \link{sketch}
containing one \code{shape_twist()} per recycled row, rather than a single
drawable.
}
\examples{
draw(shape_twist(
  x = 0, y = 0, xend = 1, yend = 0, width = 0.2,
  path_distortion = noise_bridge(seed = 7734L)
))

draw(shape_twists(x = 1:3, y = 0, xend = 2:4, yend = 1, width = 0.2))

}
\seealso{
Other 2D shapes:
\code{\link[=shape_bezier]{shape_bezier()}},
\code{\link[=shape_bezier_ribbon]{shape_bezier_ribbon()}},
\code{\link[=shape_blob]{shape_blob()}},
\code{\link[=shape_circle]{shape_circle()}},
\code{\link[=shape_ellipse]{shape_ellipse()}},
\code{\link[=shape_polygon]{shape_polygon()}},
\code{\link[=shape_raw]{shape_raw()}},
\code{\link[=shape_rectangle]{shape_rectangle()}},
\code{\link[=shape_ribbon]{shape_ribbon()}},
\code{\link[=shape_wedge]{shape_wedge()}}
}
\concept{2D shapes}
