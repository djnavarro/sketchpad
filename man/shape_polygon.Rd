% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_polygon.R
\name{shape_polygon}
\alias{shape_polygon}
\alias{shape_polygons}
\title{A regular polygon}
\usage{
shape_polygon(x = 0, y = 0, radius = 1, n = 6L, trans = trans_identity(), ...)

shape_polygons(x = 0, y = 0, radius = 1, n = 6L, trans = trans_identity(), ...)
}
\arguments{
\item{x, y}{Centroid coordinates. Default \code{0}.}

\item{radius}{Radius. Must be non-negative. Default \code{1}.}

\item{n}{Number of sides (vertices). Must be at least \code{3}. Default \code{6L}.}

\item{trans}{A \link{trans} object applied to the shape's computed points.
Default \code{\link[=trans_identity]{trans_identity()}} (no transform).}

\item{...}{Arguments passed to \code{\link[=style]{style()}}.}
}
\value{
A \link{drawable}.

For \code{shape_polygons()}, a \link{sketch}.
}
\description{
\code{shape_polygon} is a \link{drawable} defined by a centroid and a radius; its
vertices are \code{n} evenly spaced points around the circumference, giving a
regular n-gon (e.g. \code{n = 3} a triangle, \code{n = 6} a hexagon).

\code{shape_polygons()} is a vectorized version of \code{\link[=shape_polygon]{shape_polygon()}}: each
argument may be a vector, recycled against the others. The result is a
\link{sketch} containing one \code{shape_polygon()} per recycled row, rather than
a single drawable.
}
\details{
Unlike \code{\link[=shape_circle]{shape_circle()}}/\code{\link[=shape_blob]{shape_blob()}}, \code{points} does not repeat its
first vertex at the end -- there are exactly \code{n} distinct vertices, since
\code{grid::polygonGrob()} closes the path itself and a polygon has no
approximation error to hide.

Recycling uses \code{purrr::pmap()}'s own vctrs-based rules: any length-1
element is broadcast to the common length; mismatched lengths greater
than 1 raise an error.
}
\examples{
draw(shape_polygon(radius = 1, n = 3L))
draw(shape_polygon(x = 1, y = 1, radius = 0.5, n = 8L, color = "darkred"))
draw(shape_polygon(n = 5L, fill = fill_stripe(colors = c("white", "grey30"))))

# rotating a low-n polygon reorients its vertices
draw(shape_polygon(n = 4L, trans = trans_rotate(pi / 4)))

draw(shape_polygons(x = 1:3, n = c(3L, 4L, 6L)))
draw(shape_polygons(
  x = 0, y = 0, radius = seq(3, 0.5, length.out = 6), n = 6L,
  fill = rep(c("grey20", "grey90"), length.out = 6)
))

}
\seealso{
Other 2D shapes:
\code{\link[=shape_bezier]{shape_bezier()}},
\code{\link[=shape_blob]{shape_blob()}},
\code{\link[=shape_circle]{shape_circle()}},
\code{\link[=shape_ellipse]{shape_ellipse()}},
\code{\link[=shape_raw]{shape_raw()}},
\code{\link[=shape_rectangle]{shape_rectangle()}},
\code{\link[=shape_ribbon]{shape_ribbon()}},
\code{\link[=shape_ribbonpath]{shape_ribbonpath()}},
\code{\link[=shape_stroke]{shape_stroke()}},
\code{\link[=shape_twist]{shape_twist()}},
\code{\link[=shape_wedge]{shape_wedge()}}
}
\concept{2D shapes}
