% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.R
\name{fill_scribble}
\alias{fill_scribble}
\title{Wandering-line scribble texture fill}
\usage{
fill_scribble(
  color = "black",
  direction = c("horizontal", "vertical"),
  n_lines = 5L,
  n_harmonics = 3L,
  amplitude = 0.35,
  resolution = 200L,
  linewidth = 1,
  spacing = 0.25,
  aspect = 1,
  seed = 1L,
  extend = "repeat"
)
}
\arguments{
\item{color}{Line colour. Default \code{"black"}.}

\item{direction}{Either \code{"horizontal"} (lines run left-right) or
\code{"vertical"} (lines run top-bottom). Default \code{"horizontal"}.}

\item{n_lines}{Number of wandering lines per tile. Must be a positive
integer. Default \code{5L}.}

\item{n_harmonics}{Number of sine harmonics summed per line. Must be a
positive integer. Default \code{3L}.}

\item{amplitude}{Maximum total wiggle amplitude, as a \code{"npc"} fraction
of the tile, split across \code{n_harmonics} (so more harmonics each
contribute proportionally less). Must be a non-negative number.
Default \code{0.35}.}

\item{resolution}{Number of points sampled along each line. Must be a
positive integer of at least \code{2L}. Default \code{200L}.}

\item{linewidth}{Line width. Must be a positive number. Default \code{1}.}

\item{spacing}{Baseline tile size, as a fraction of the target's bounding
box. Must be a positive number. Default \code{0.25}.}

\item{aspect}{Width-to-height ratio of the target polygon's bounding box.
Must be a positive number. Default \code{1} (a square bounding box).}

\item{seed}{Integer seed for the random harmonics. Default \code{1L}.}

\item{extend}{Passed to \code{\link[grid:pattern]{grid::pattern()}}. Default \code{"repeat"}.}
}
\value{
A pattern object as returned by \code{\link[grid:pattern]{grid::pattern()}}, suitable for
use as the \code{fill} argument to \code{\link[grid:gpar]{grid::gpar()}}.
}
\description{
\code{fill_scribble()} builds a \code{\link[grid:pattern]{grid::pattern()}} fill value from several
randomly wandering lines, each a random finite sum of sine harmonics
(see the internal \code{scribble_lines()} helper) rather than a smooth noise
field or a scattered discrete motif -- giving a loose, hand-drawn
scribble texture built from genuinely continuous strokes.
}
\details{
A wandering \emph{open} line poses a tiling problem none of the other
\verb{fill_*()} helpers have: \code{\link[=fill_hatch]{fill_hatch()}}'s diagonal and \code{\link[=fill_noise]{fill_noise()}}'s
raster both tile by construction (a straight corner-to-corner line, or
an already-periodic field), and \code{\link[=fill_stipple]{fill_stipple()}}/\code{\link[=fill_scatter]{fill_scatter()}}/
\code{\link[=fill_halftone]{fill_halftone()}} sidestep the problem entirely by keeping their
scattered content margined well clear of the tile edge. A wandering
line that's meant to look continuous \emph{can't} stay clear of the edge --
it has to run all the way to it, and pick up again at exactly the right
place, in both position and slope, on the opposite edge, or the seam
shows as a visible kink. \code{fill_scribble()} gets this for free by
building each line as a random sum of sine harmonics at \emph{integer}
frequencies only: over one full period, such a sum always returns
exactly to its starting value and slope (to floating-point precision),
so consecutive tile copies join with no visible seam -- confirmed
visually with \code{extend = "repeat"} at small \code{spacing}, including
repeated copies (unlike the polygon-in-a-genuinely-repeated-tile issue
documented at \code{\link[=fill_scatter]{fill_scatter()}}, open-line content showed no clipping or
distortion in testing).
}
\section{Known limitation -- direction is fixed, not an arbitrary angle}{

Every other angled helper (\code{\link[=fill_hatch]{fill_hatch()}}/\code{\link[=fill_crosshatch]{fill_crosshatch()}}/
\code{\link[=fill_stripe]{fill_stripe()}}) achieves an arbitrary angle by reshaping the \emph{tile}
itself (via \code{hatch_tile_dims()}) around content that's a plain
corner-to-corner diagonal. That trick was tried here first and found
not to generalize: reshaping the tile around a \emph{wandering} line just
anisotropically stretches its wiggle rather than rotating it, since
the line's content isn't a bare diagonal the tile shape can
reinterpret. A genuinely rotated wandering line would need the tile
built as a rotated/sheared parallelogram with edge-matching worked
out for a curve rather than a segment -- no such technique exists in
this package yet. \code{direction} is therefore restricted to \code{"horizontal"}
(lines run left-right, periodic tiling along that axis) or
\code{"vertical"} (lines run top-bottom instead, i.e. \code{along}/\code{across} from
\code{scribble_lines()} mapped to \code{y}/\code{x} rather than \code{x}/\code{y}) -- there is
no \code{angle} argument. Revisit if a real sketch needs an arbitrary
angle.
}

\seealso{
Other fill helpers:
\code{\link[=fill_checker]{fill_checker()}},
\code{\link[=fill_crosshatch]{fill_crosshatch()}},
\code{\link[=fill_flow]{fill_flow()}},
\code{\link[=fill_gradient]{fill_gradient()}},
\code{\link[=fill_halftone]{fill_halftone()}},
\code{\link[=fill_hatch]{fill_hatch()}},
\code{\link[=fill_image]{fill_image()}},
\code{\link[=fill_marble]{fill_marble()}},
\code{\link[=fill_noise]{fill_noise()}},
\code{\link[=fill_none]{fill_none()}},
\code{\link[=fill_scatter]{fill_scatter()}},
\code{\link[=fill_solid]{fill_solid()}},
\code{\link[=fill_stipple]{fill_stipple()}},
\code{\link[=fill_stripe]{fill_stripe()}},
\code{\link[=fill_vignette]{fill_vignette()}}
}
\concept{fill helpers}
