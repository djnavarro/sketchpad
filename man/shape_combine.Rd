% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_combine.R
\name{shape_combine}
\alias{shape_combine}
\title{Combine several drawables into one multi-sub-path shape}
\usage{
shape_combine(..., style = NULL)
}
\arguments{
\item{...}{Two or more polygon-geometry \link{drawable} objects (\verb{@geometry == "polygon"}, e.g. \code{\link[=shape_circle]{shape_circle()}}, \code{\link[=shape_blob]{shape_blob()}}).}

\item{style}{A \link{style} object for the combined shape, or \code{NULL} (the
default) to reuse the first input's own \code{style}. Every input's own
style besides the one that's kept is discarded -- a \code{drawable} has
exactly one \code{style}; several independently-styled shapes should use
a \link{sketch} instead.}
}
\value{
A \link{shape_raw}.
}
\description{
\code{shape_combine()} merges the computed \code{points} of several
polygon-geometry drawables into a single \link{shape_raw}, one sub-path per
input (see \link{xy}'s own \code{id}). This is the ergonomic entry point for the
two motivating multi-sub-path use cases -- a shape with a hole (one
input nested inside another, under \code{\link[=style]{style()}}'s default \code{rule = "evenodd"}), or several disjoint shapes sharing one \link{style} -- without
computing \code{id} by hand the way \code{\link[=shape_raw]{shape_raw()}}'s own \code{id} argument
requires.
}
\details{
Each input's own already-computed \code{points} (i.e. \code{trans}/noise-based
distortion already applied, the same way \code{\link[S7:convert]{convert()}} "bakes in" a
drawable's transform) becomes one or more sub-paths in the combined
output -- an input that already has multiple sub-paths of its own
(e.g. the output of an earlier \code{shape_combine()} call) keeps them all,
renumbered to stay distinct from every other input's own sub-paths.
Whether nested sub-paths render as a hole or a second solid region is
purely a function of their geometric nesting and \code{style@rule} (see
\code{\link[=style]{style()}}'s own docs) -- not of argument order, and not of anything
\code{shape_combine()} itself decides.
}
\examples{
# a ring with a hole: a smaller circle nested inside a larger one
draw(shape_combine(
  shape_circle(radius = 2),
  shape_circle(radius = 1),
  style = style(fill = "steelblue")
))

# several disjoint blobs sharing one style
draw(shape_combine(
  shape_blob(x = 0, distortion = noise_field(seed = 1L)),
  shape_blob(x = 3, distortion = noise_field(seed = 2L)),
  shape_blob(x = 6, distortion = noise_field(seed = 3L)),
  style = style(fill = "goldenrod")
))

# combining a hole with a disjoint extra shape in one call
draw(shape_combine(
  shape_circle(x = 0, radius = 2),
  shape_circle(x = 0, radius = 1),
  shape_circle(x = 4, radius = 0.5)
))

}
\seealso{
Other 2D shapes:
\code{\link[=shape_bezier]{shape_bezier()}},
\code{\link[=shape_blob]{shape_blob()}},
\code{\link[=shape_circle]{shape_circle()}},
\code{\link[=shape_ellipse]{shape_ellipse()}},
\code{\link[=shape_polygon]{shape_polygon()}},
\code{\link[=shape_raw]{shape_raw()}},
\code{\link[=shape_rectangle]{shape_rectangle()}},
\code{\link[=shape_ribbon]{shape_ribbon()}},
\code{\link[=shape_star]{shape_star()}},
\code{\link[=shape_stroke]{shape_stroke()}},
\code{\link[=shape_strokepath]{shape_strokepath()}},
\code{\link[=shape_twist]{shape_twist()}},
\code{\link[=shape_wedge]{shape_wedge()}}
}
\concept{2D shapes}
