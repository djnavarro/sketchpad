% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.R
\name{fill_marble}
\alias{fill_marble}
\title{Marbled, veined texture fill}
\usage{
fill_marble(
  color1 = "white",
  color2 = "black",
  spacing = 0.5,
  aspect = 1,
  resolution = 32L,
  stripes = 3L,
  warp = 4,
  frequency = 1,
  octaves = 3L,
  seed = 1L,
  extend = "repeat"
)
}
\arguments{
\item{color1, color2}{The two colours blended across each band. Defaults
\code{"white"} and \code{"black"}.}

\item{spacing}{Baseline tile size, as a fraction of the target's bounding
box. Must be a positive number. Default \code{0.5}.}

\item{aspect}{Width-to-height ratio of the target polygon's bounding box.
Must be a positive number. Default \code{1} (a square bounding box).}

\item{resolution}{Raster resolution (pixels per tile edge). Must be a
positive integer of at least \code{2L}. Default \code{32L}.}

\item{stripes}{Number of bands running around the tile's \code{u} axis before
turbulence displacement. Must be a positive integer. Default \code{3L}.}

\item{warp}{Turbulence amplitude, in radians of displacement along the
band coordinate. Must be a non-negative number. Default \code{4}.}

\item{frequency}{Noise frequency, as in \code{\link[=shape_blob]{shape_blob()}}. Must be non-negative.
Default \code{1}.}

\item{octaves}{Number of turbulence octaves, as in \code{\link[=shape_blob]{shape_blob()}}. Must be a
positive integer. Default \code{3L}.}

\item{seed}{Integer seed for the noise field, as in \code{\link[=shape_blob]{shape_blob()}}. Default
\code{1L}.}

\item{extend}{Passed to \code{\link[grid:pattern]{grid::pattern()}}. Default \code{"repeat"}.}
}
\value{
A pattern object as returned by \code{\link[grid:pattern]{grid::pattern()}}, suitable for
use as the \code{fill} argument to \code{\link[grid:gpar]{grid::gpar()}}.
}
\description{
\code{fill_marble()} builds a \code{\link[grid:pattern]{grid::pattern()}} fill value resembling veined
marble: a set of \code{stripes} parallel bands running around the tile's \code{u}
axis, displaced by a \code{\link[=fill_noise]{fill_noise()}}-style torus-periodic turbulence
field (via the shared internal \code{torus_grid()}/\code{torus_noise()} helpers)
rather than left straight -- the classic "sine of a coordinate plus
turbulence" recipe for procedural marble.
}
\details{
Periodicity needs two things, both already true here: the undisplaced
bands (\code{sin(theta_u * stripes)}) are periodic in \code{u} for any \emph{integer}
\code{stripes}, since \code{theta_u} itself advances by exactly \code{2 * pi} over one
tile width; and the turbulence added on top is periodic in both \code{u} and
\code{v} because it comes from \code{torus_noise()}, the same torus-sampling
technique \code{\link[=fill_noise]{fill_noise()}} uses for its own field. Adding one periodic
function to another (here, inside a further \code{sin()}) stays periodic, so
the combined result still tiles with no seam.

Unlike \code{\link[=fill_noise]{fill_noise()}} (opacity of one colour), the banding is rendered
as a blend between \code{color1} and \code{color2}, since a marble texture's
visual interest is the veining pattern itself rather than a fade to
transparency.

\code{\link[=fill_noise]{fill_noise()}}'s own faint tile-boundary rasterization seam (see its
details) can be more noticeable here: \code{sin()} turns a small mismatch in
the turbulence field into a visibly sharper edge in a band than the
same mismatch would produce in a plain opacity fade.
}
\examples{
draw(shape_circle(fill = fill_marble(stripes = 4L, seed = 1274L)))

}
\seealso{
Other fill helpers:
\code{\link[=fill_checker]{fill_checker()}},
\code{\link[=fill_crosshatch]{fill_crosshatch()}},
\code{\link[=fill_flow]{fill_flow()}},
\code{\link[=fill_gradient]{fill_gradient()}},
\code{\link[=fill_halftone]{fill_halftone()}},
\code{\link[=fill_hatch]{fill_hatch()}},
\code{\link[=fill_image]{fill_image()}},
\code{\link[=fill_noise]{fill_noise()}},
\code{\link[=fill_none]{fill_none()}},
\code{\link[=fill_scatter]{fill_scatter()}},
\code{\link[=fill_scribble]{fill_scribble()}},
\code{\link[=fill_solid]{fill_solid()}},
\code{\link[=fill_stipple]{fill_stipple()}},
\code{\link[=fill_stripe]{fill_stripe()}},
\code{\link[=fill_vignette]{fill_vignette()}}
}
\concept{fill helpers}
