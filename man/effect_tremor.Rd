% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effect_tremor.R
\name{effect_tremor}
\alias{effect_tremor}
\title{Layer jittered copies of a drawable for a hand-drawn look}
\usage{
effect_tremor(
  object,
  layers = 4L,
  jitter = 0.05,
  jitter_frequency = 0.5,
  seed = 1L
)
}
\arguments{
\item{object}{A pathlike \link{drawable} (\verb{@pathlike == TRUE}, see
\link{drawable}'s docs), e.g. \code{\link[=curve_line]{curve_line()}}, \code{\link[=shape_stroke]{shape_stroke()}},
\code{\link[=shape_bezier]{shape_bezier()}}. Every other property is preserved unchanged
across layers.}

\item{layers}{Number of independently-jittered copies to layer. Must
be a positive integer. Default \code{4L}.}

\item{jitter}{Maximum displacement amplitude applied to each layer's
\code{x}/\code{y}. Must be non-negative. Default \code{0.05}.}

\item{jitter_frequency}{Frequency of the simplex noise driving the
jitter, sampled along the path's normalized arc-length -- lower
values give a slower, smoother wobble; higher values a jumpier one.
Must be non-negative. Default \code{0.5}.}

\item{seed}{Integer seed for the jitter noise. Default \code{1L}.}
}
\value{
A \link{sketch} containing \code{layers} drawables.
}
\description{
\code{effect_tremor()} builds a \link{sketch} of \code{layers} such copies: each is a
copy of \code{object} (via \code{\link[S7:set_props]{S7::set_props()}}) with its \code{x}/\code{y} displaced by
smooth, seed-offset simplex noise sampled along the path's own
normalized arc-length.
}
\details{
No single \link{drawable} can express a hand-drawn ink/pencil look on its
own -- what reads as hand-drawn is several independently wobbling
copies of the same nominal path layered on top of each other, not one
perfectly smooth line. This is the same ad hoc technique used, during
development, to add a wobbling pencil edge on top of a
\code{\link[=shape_stroke]{shape_stroke()}}'s tapered outline, or to layer a plain \code{\link[=curve_line]{curve_line()}}
into a shakier-looking line by itself.

Noise is sampled at each control point's own position along arc-length
(\code{0} to \code{1}), not at its raw \code{x}/\code{y} coordinates, so the jitter's shape
is independent of the path's own scale or aspect ratio -- a long,
shallow path and a short, steep one with the same number of control
points get comparably-shaped wobble. Each layer's noise is
seed-offset from the last (\code{seed + 2 * (i - 1)} for the \code{x}
displacement, one more for \code{y}, following the same seed-offset
convention \code{trans_warp()}/\code{twisted_path_points()} already use for
their own independent-axis noise), so layers wobble independently
rather than moving in lockstep.

Because each layer is built with \code{\link[S7:set_props]{S7::set_props()}} from \code{object}
itself, every other property -- style, \code{width}/\code{distortion} for a
\code{\link[=shape_stroke]{shape_stroke()}}, \code{trans}, ... -- carries over unchanged;
\code{effect_tremor()} only varies \code{x}/\code{y} across layers. Vary \code{color_alpha}
down and/or \code{layers} up (on \code{object} and via the \code{layers} argument,
respectively) for a denser, more overlapping pencil/ink texture.
}
\examples{
template <- curve_line(x = c(0, 1, 2, 3), y = c(0, 1, 0, 1))
faded <- curve_line(x = c(0, 1, 2, 3), y = c(0, 1, 0, 1), color_alpha = 0.4)

# before: a single crisp line
draw(template)

# after: several jittered, faded copies read as hand-drawn
draw(effect_tremor(faded))

draw(effect_tremor(
  shape_stroke(
    x = c(0, 1, 2, 3), y = c(0, 1, 0, 1),
    width = 0.3, fill_alpha = 0.5, color = NA_character_
  ),
  layers = 3L, jitter = 0.03
))

# more layers and higher jitter give a denser, shakier scribble
draw(effect_tremor(faded, layers = 10L, jitter = 0.15))

}
\seealso{
Other effects:
\code{\link[=effect_bristle]{effect_bristle()}},
\code{\link[=effect_grain]{effect_grain()}}
}
\concept{effects}
