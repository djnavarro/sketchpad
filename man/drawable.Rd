% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drawable.R
\name{drawable}
\alias{drawable}
\title{Parent class for all drawable objects}
\usage{
drawable(..., geometry = "polygon", trans = trans_identity(), pathlike = FALSE)
}
\arguments{
\item{...}{Arguments passed to \code{\link[=style]{style()}}.}

\item{geometry}{One of \code{"polygon"} (default), \code{"path"}, or \code{"points"}.
Not exposed as a constructor argument by any concrete drawable --
each \verb{shape_*()}/\verb{curve_*()}/\code{points_raw()} constructor fixes one
value internally instead (see details).}

\item{trans}{A \link{trans}/\link{trans_warp}/\link{trans_chain} object. See details.}

\item{pathlike}{A single \code{TRUE}/\code{FALSE} (default \code{FALSE}). Not exposed
as a constructor argument by any concrete drawable -- each fixes its
own value internally, the same convention \code{geometry} follows. See
details.}
}
\description{
\code{drawable} enforces structure on its subclasses: every drawable must
carry a \link{style}, a \code{geometry}, and expose a computed \code{points} property, of
class \link{xy}. It is not intended to be instantiated directly; use one
of its subclasses (\link{shape_raw}, \link{shape_circle}, \link{shape_blob},
\link{shape_ribbon}, \link{shape_twist}, \link{curve_raw}, \link{points_raw}, ...) instead.
}
\details{
\code{geometry} tells \code{\link[=draw]{draw()}} which \link{grid} grob a drawable's \code{points} map to,
following a dimensional reading: \code{"points"} (0D, \code{\link[grid:pointsGrob]{grid::pointsGrob()}},
e.g. \code{\link[=points_raw]{points_raw()}}), \code{"path"} (1D, an open \code{\link[grid:polylineGrob]{grid::polylineGrob()}}, e.g.
\code{\link[=curve_line]{curve_line()}}/\code{\link[=curve_raw]{curve_raw()}}), or \code{"polygon"} (2D, a closed
\code{\link[grid:polygonGrob]{grid::polygonGrob()}} -- the default, and the only value any
\verb{shape_*()} constructor uses). \code{style@fill} is ignored for
\code{"points"}/\code{"path"} geometries, since only a closed polygon has an
interior to fill.

\code{trans} is a \link{trans} (an affine map: \code{\link[=trans_translate]{trans_translate()}},
\code{\link[=trans_rotate]{trans_rotate()}}, \code{\link[=trans_scale]{trans_scale()}}, \code{\link[=trans_reflect]{trans_reflect()}}, \code{\link[=trans_shear]{trans_shear()}},
\code{\link[=trans_affine]{trans_affine()}}), a \link{trans_warp} (a non-rigid, noise-based
deformation), or a \link{trans_chain} combining several via \code{+}, applied to
a drawable's computed \code{points} as the very last step -- after any
shape-specific geometry (and, for \code{\link[=shape_blob]{shape_blob()}}/\code{\link[=shape_ribbon]{shape_ribbon()}}/
\code{\link[=shape_twist]{shape_twist()}}, any noise-based distortion) has already been computed.
This means a drawable's own defining parameters (e.g. \code{\link[=shape_circle]{shape_circle()}}'s
centroid/radius) are never mutated or flattened by a transform -- only
the final rendered coordinates are affected. Default \code{\link[=trans_identity]{trans_identity()}}
(no transform).

\code{pathlike} marks whether \code{x}/\code{y} (where present) hold a genuine,
caller-ordered, perturbable control-point path -- as opposed to \code{x}/\code{y}
meaning something else entirely (e.g. \code{\link[=shape_circle]{shape_circle()}}'s centroid, or
one fixed endpoint of \code{\link[=shape_ribbon]{shape_ribbon()}}'s two-point segment). This is
the distinction \code{\link[=effect_tremor]{effect_tremor()}}/\code{\link[=effect_bristle]{effect_bristle()}} need to decide whether
jittering \code{x}/\code{y} produces a meaningful wobble; it's orthogonal to
\code{geometry} -- a \code{pathlike} drawable can have any \code{geometry} (a future
\verb{points_*()} constructor could reasonably be \code{pathlike} despite
\code{geometry == "points"}). Currently \code{TRUE} for \code{\link[=shape_raw]{shape_raw()}},
\code{\link[=curve_raw]{curve_raw()}}, \code{\link[=curve_line]{curve_line()}}, \code{\link[=shape_stroke]{shape_stroke()}}, \code{\link[=shape_bezier]{shape_bezier()}},
\code{\link[=curve_bezier]{curve_bezier()}}, and \code{\link[=points_raw]{points_raw()}}; \code{FALSE} (the default) for every
other concrete drawable, including \code{\link[=shape_ribbon]{shape_ribbon()}}/\code{\link[=shape_twist]{shape_twist()}}/
\code{\link[=curve_twist]{curve_twist()}} -- these do have a conceptual
backbone, but it's exposed via \code{x}/\code{y}/\code{xend}/\code{yend} (or additional
named control-point pairs), not a plain \code{x}/\code{y} vector. Whether a
\code{pathlike} subclass actually has \code{x}/\code{y} properties is not enforced by
\code{drawable}'s own validator -- every subclass constructor first builds
a scaffold \code{drawable()} instance (validated on its own, before any
subclass property exists) and only merges in \code{x}/\code{y} afterward via
\code{S7::new_object()}, so a cross-property check here would fire on that
scaffold and reject every \code{pathlike} subclass unconditionally (see
"Gotchas"). Setting \code{pathlike = TRUE} on a subclass with no \code{x}/\code{y} is
therefore an author error caught only when an effect tries to read
\code{object@x}/\code{object@y}, not at construction time.
}
\examples{
circ <- shape_circle(radius = 1)
S7::S7_inherits(circ, drawable)

# geometry controls which grob draw() builds: a closed outline, an open
# stroke, or unconnected markers
draw(shape_circle(radius = 1, n = 8))
draw(curve_line(x = c(0, 1, 2, 3), y = c(0, 1, 0, 1), linewidth = 3))
draw(points_raw(x = runif(30), y = runif(30)))

# trans applies after a shape's own geometry/distortion is computed, so
# shape_rectangle()'s centroid/width/height stay fixed -- only the final
# rendered corners rotate
draw(shape_rectangle(
  width = 1.5,
  height = 0.5,
  trans = trans_rotate(pi / 6)
))

}
\seealso{
Other core structure:
\code{\link[=canvas]{canvas()}},
\code{\link[=draw]{draw()}},
\code{\link[=sketch]{sketch()}},
\code{\link[=style]{style()}},
\code{\link[=xy]{xy()}}
}
\concept{core structure}
