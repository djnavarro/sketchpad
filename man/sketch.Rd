% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sketch.R
\name{sketch}
\alias{sketch}
\title{A collection of drawable objects}
\usage{
sketch(shapes = list(), canvas = sketchpad::canvas())
}
\arguments{
\item{shapes}{A list of \link{drawable}-classed objects. Default \code{list()}.}

\item{canvas}{A \link{canvas} object, giving the background/framing settings
\code{\link[=draw]{draw()}} applies to the sketch as a whole. Default \code{canvas()} (no
background, axis limits computed from \code{shapes}).}
}
\description{
\code{sketch} is a list of \link{drawable} objects that can be rendered together
with a single call to \code{\link[=draw]{draw()}}. Sketches can be built up incrementally
using the \code{+} operator, e.g.
\code{sketch() + shape_circle() + shape_circle(x = 2)}.
}
\details{
A sketch also supports list-like access to its shapes: \code{length(s)}
counts them, \code{s[[i]]} returns the single \link{drawable} at position \code{i},
and \code{s[i]} returns a new sketch containing only the selected shapes
(its \code{canvas} is preserved) -- mirroring how \code{[[}/\code{[} differ on a
plain list.
}
\examples{
s <- sketch() + shape_circle(radius = 1) + shape_circle(x = 2, radius = 0.5)
draw(s)

s2 <- sketch(canvas = canvas(background = "grey95")) +
  shape_circle(radius = 1)
draw(s2)

length(s)
s[[1]]
s[1]

# a trans applied to a sketch composes onto every shape's own @trans
draw(s + trans_rotate(pi / 6))

# accumulate many shapes in a loop, e.g. a ring of circles
ring <- sketch()
for (angle in seq(0, 2 * pi, length.out = 9)[-9]) {
  ring <- ring + shape_circle(x = cos(angle), y = sin(angle), radius = 0.3)
}
draw(ring)

}
\seealso{
Other core structure:
\code{\link[=canvas]{canvas()}},
\code{\link[=draw]{draw()}},
\code{\link[=drawable]{drawable()}},
\code{\link[=group]{group()}},
\code{\link[=style]{style()}},
\code{\link[=xy]{xy()}}
}
\concept{core structure}
