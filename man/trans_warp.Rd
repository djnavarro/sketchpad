% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trans.R
\name{trans_warp}
\alias{trans_warp}
\title{A non-rigid, noise-based deformation}
\usage{
trans_warp(
  amount = 0.1,
  distortion_x = noise_field(),
  distortion_y = noise_field(seed = distortion_x@seed + 1L)
)
}
\arguments{
\item{amount}{Displacement amplitude. Must be non-negative. Default
\code{0.1}.}

\item{distortion_x}{A \link{noise_field} controlling the x displacement.
Default \code{noise_field()}.}

\item{distortion_y}{A \link{noise_field} controlling the y displacement.
Default \code{distortion_x}, with its seed offset by \code{1}.}
}
\description{
\code{trans_warp} displaces each point by simplex/fractal noise sampled at
the point's own \verb{(x, y)} position (domain warping), giving a wobbly,
non-rigid distortion rather than an affine map.
}
\details{
Unlike \link{trans} (translate/rotate/scale/reflect/shear), this can't be
represented as a single matrix, since the displacement varies smoothly
but irregularly from point to point.

The x and y displacements are sampled from two independent
\link{noise_field}s (\code{distortion_x}/\code{distortion_y}), each rescaled to
\verb{[-1, 1]} and multiplied by \code{amount}. By default \code{distortion_y} reuses
\code{distortion_x}'s own settings with its seed offset by \code{1}, so the two
axes wander independently without the caller needing to specify two
full \link{noise_field} objects -- the same convention
\code{twisted_path_points()} uses internally for \code{\link[=shape_twist]{shape_twist()}}'s path.

Like \link{trans}, a \code{trans_warp} is attached to a \link{drawable} via its
\code{trans} property/argument, and composes with \code{+} -- combining it with
another \code{trans_warp} or a \link{trans} produces a \link{trans_chain}, since a
non-rigid warp can't collapse into a single matrix.
}
\examples{
draw(shape_circle(radius = 1, n = 200, trans = trans_warp(amount = 0.15)))

# a smaller amount gives a subtler wobble; distortion_x's own frequency
# controls how quickly the warp varies across space
draw(shape_circle(radius = 1, n = 200, trans = trans_warp(amount = 0.03)))
draw(shape_circle(
  radius = 1, n = 200,
  trans = trans_warp(amount = 0.1, distortion_x = noise_field(frequency = 4))
))

}
\seealso{
Other transform helpers:
\code{\link[=trans]{trans()}},
\code{\link[=trans_affine]{trans_affine()}},
\code{\link[=trans_chain]{trans_chain()}},
\code{\link[=trans_identity]{trans_identity()}},
\code{\link[=trans_reflect]{trans_reflect()}},
\code{\link[=trans_rotate]{trans_rotate()}},
\code{\link[=trans_scale]{trans_scale()}},
\code{\link[=trans_shear]{trans_shear()}},
\code{\link[=trans_translate]{trans_translate()}}
}
\concept{transform helpers}
