% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette.R
\name{palette_manual}
\alias{palette_manual}
\title{Colour palette drawn from a curated collection}
\usage{
palette_manual(n = NULL, index = 1L)
}
\arguments{
\item{n}{\code{NULL} (the default) or a single positive whole number: the
number of colours to return. \code{NULL} returns the selected palette's own
colours unchanged; otherwise the palette is interpolated to \code{n}
colours.}

\item{index}{A single positive whole number selecting which stored
palette to use. An out-of-range \code{index} errors with the valid range
for the currently vendored data.}
}
\value{
A character vector of \code{n} hex colour strings (or the selected
palette's own colours, if \code{n} is \code{NULL}).
}
\description{
Selects one palette from a vendored, deduplicated copy of the manually
curated palettes at \url{https://github.com/djnavarro/palettes}, each
originally a 5-colour hex palette. If \code{n} differs from the palette's own
native length, colours are linearly interpolated to \code{n} colours via
\code{\link[grDevices:colorRampPalette]{grDevices::colorRampPalette()}}.
}
\examples{
palette_manual(index = 1)
palette_manual(n = 12, index = 1)

}
\seealso{
Other palette helpers:
\code{\link[=palette_cosine]{palette_cosine()}}
}
\concept{palette helpers}
