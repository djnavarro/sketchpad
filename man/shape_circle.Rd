% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_circle.R
\name{shape_circle}
\alias{shape_circle}
\alias{shape_circles}
\title{A circle}
\usage{
shape_circle(x = 0, y = 0, radius = 1, n = 100L, trans = trans_identity(), ...)

shape_circles(
  x = 0,
  y = 0,
  radius = 1,
  n = 100L,
  trans = trans_identity(),
  ...
)
}
\arguments{
\item{x, y}{Centroid coordinates. Default \code{0}.}

\item{radius}{Radius. Must be non-negative. Default \code{1}.}

\item{n}{Number of points used to approximate the circle. Default \code{100L}.}

\item{trans}{A \link{trans} object applied to the shape's computed points.
Default \code{\link[=trans_identity]{trans_identity()}} (no transform).}

\item{...}{Arguments passed to \code{\link[=style]{style()}}.}
}
\value{
A \link{drawable}.

For \code{shape_circles()}, a \link{sketch}.
}
\description{
\code{shape_circle} is a \link{drawable} defined by a centroid and a radius; its
vertices are computed as evenly spaced points around the circumference.

\code{shape_circles()} is a vectorized version of \code{\link[=shape_circle]{shape_circle()}}: each
argument may be a vector, recycled against the others. The result is a
\link{sketch} containing one \code{shape_circle()} per recycled row, rather than
a single drawable.
}
\details{
Recycling uses \code{purrr::pmap()}'s own vctrs-based rules: any length-1
element is broadcast to the common length; mismatched lengths greater
than 1 raise an error.
}
\examples{
draw(shape_circle(radius = 1))
draw(shape_circle(x = 1, y = 1, radius = 0.5, n = 6, color = "darkred"))
draw(shape_circle(radius = 1, fill = "steelblue", color = NA_character_))
draw(shape_circle(radius = 1, fill = fill_hatch(angle = 30)))

draw(shape_circles(x = 1:3, radius = c(0.5, 1, 1.5)))

# each argument recycles independently, so colour can vary alongside position
draw(shape_circles(
  x = cos(seq(0, 2 * pi, length.out = 9))[-9],
  y = sin(seq(0, 2 * pi, length.out = 9))[-9],
  radius = 0.3,
  fill = rep(c("tomato", "steelblue"), length.out = 8)
))

}
\seealso{
Other 2D shapes:
\code{\link[=shape_bezier]{shape_bezier()}},
\code{\link[=shape_blob]{shape_blob()}},
\code{\link[=shape_ellipse]{shape_ellipse()}},
\code{\link[=shape_polygon]{shape_polygon()}},
\code{\link[=shape_raw]{shape_raw()}},
\code{\link[=shape_rectangle]{shape_rectangle()}},
\code{\link[=shape_ribbon]{shape_ribbon()}},
\code{\link[=shape_stroke]{shape_stroke()}},
\code{\link[=shape_strokepath]{shape_strokepath()}},
\code{\link[=shape_twist]{shape_twist()}},
\code{\link[=shape_wedge]{shape_wedge()}}
}
\concept{2D shapes}
