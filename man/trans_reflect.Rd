% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trans.R
\name{trans_reflect}
\alias{trans_reflect}
\title{Reflect points}
\usage{
trans_reflect(x = FALSE, y = FALSE, about_x = 0, about_y = 0)
}
\arguments{
\item{x, y}{Whether to flip the x/y coordinate. Default \code{FALSE} for both
(i.e. the identity transform).}

\item{about_x, about_y}{Pivot point coordinates. Default \code{0}.}
}
\description{
Flips points across a vertical and/or horizontal line through the pivot
\verb{(about_x, about_y)}: \code{x = TRUE} mirrors left-right (flips the
x-coordinate), \code{y = TRUE} mirrors top-bottom (flips the y-coordinate).
Setting both reflects through the pivot point itself (equivalent to a
half turn, i.e. \code{trans_rotate(pi, about_x, about_y)}).
}
\details{
Reflection across an arbitrary-angle line isn't exposed as a separate
argument -- it composes from existing pieces instead: rotate the desired
line onto an axis, reflect, then rotate back, e.g.
\code{trans_rotate(-theta) + trans_reflect(y = TRUE) + trans_rotate(theta)}
reflects across a line through the origin at angle \code{theta}.
}
\examples{
draw(shape_bezier(x = c(0, 0.5, 1), y = c(0, 1, 0.2), trans = trans_reflect(x = TRUE)))

# overlay the original (faded) with each reflected copy (solid)
original <- shape_rectangle(x = 1, width = 1, height = 0.4, fill_alpha = 0.3, color_alpha = 0.3)
draw(sketch() + original + (original + trans_reflect(x = TRUE)))
draw(sketch() + original + (original + trans_reflect(y = TRUE)))

# reflecting through both axes is equivalent to a half turn
draw(shape_bezier(
  x = c(0, 0.5, 1), y = c(0, 1, 0.2),
  trans = trans_reflect(x = TRUE, y = TRUE)
))

}
\seealso{
Other transform helpers:
\code{\link[=trans]{trans()}},
\code{\link[=trans_affine]{trans_affine()}},
\code{\link[=trans_chain]{trans_chain()}},
\code{\link[=trans_identity]{trans_identity()}},
\code{\link[=trans_rotate]{trans_rotate()}},
\code{\link[=trans_scale]{trans_scale()}},
\code{\link[=trans_shear]{trans_shear()}},
\code{\link[=trans_translate]{trans_translate()}},
\code{\link[=trans_warp]{trans_warp()}}
}
\concept{transform helpers}
