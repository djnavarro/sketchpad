% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/noise_field.R
\name{noise_sample}
\alias{noise_sample}
\title{Sample a noise object}
\usage{
noise_sample(field, ...)
}
\arguments{
\item{field}{A noise object, e.g. one built by \code{\link[=noise_field]{noise_field()}}.}

\item{...}{Passed to the method for \code{field}'s class.}
}
\value{
A numeric vector.
}
\description{
\code{noise_sample()} evaluates a noise object at a set of positions and
returns the (typically rescaled) sampled values.
}
\details{
It is an S7 generic dispatching on \code{field}; each concrete noise class
implements its own method, since what "a position" means differs by
class -- a \link{noise_field} is sampled at arbitrary \verb{(x, y)} coordinates
in the plane, matching \code{\link[ambient:fracture]{ambient::fracture()}}'s own interface, while a
\link{noise_bridge} instead samples by point count alone, with no
\verb{(x, y)} positions involved.
}
\examples{
noise_sample(noise_field(seed = 4821), x = 1:5, y = 1:5, to = c(0, 1))

# noise_bridge()'s method samples by point count instead of position
noise_sample(noise_bridge(seed = 4821), n = 5, scale = 1)

# sampled values can drive a drawable's own geometry, e.g. shape_blob()'s
# radius perturbation (see its `points` getter)
angle <- seq(0, 2 * pi, length.out = 12)
noise_sample(
  noise_field(seed = 4821),
  x = cos(angle),
  y = sin(angle),
  to = c(0.8, 1.2)
)

}
\seealso{
Other noise helpers:
\code{\link[=noise_bridge]{noise_bridge()}},
\code{\link[=noise_field]{noise_field()}}
}
\concept{noise helpers}
