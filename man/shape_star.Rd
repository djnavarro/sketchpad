% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_star.R
\name{shape_star}
\alias{shape_star}
\alias{shape_stars}
\title{A star}
\usage{
shape_star(
  x = 0,
  y = 0,
  outer_radius = 1,
  inner_radius = 0.5,
  n = 5L,
  trans = trans_identity(),
  ...
)

shape_stars(
  x = 0,
  y = 0,
  outer_radius = 1,
  inner_radius = 0.5,
  n = 5L,
  trans = trans_identity(),
  ...
)
}
\arguments{
\item{x, y}{Centroid coordinates. Default \code{0}.}

\item{outer_radius}{Outer radius (star tips). Must be non-negative.
Default \code{1}.}

\item{inner_radius}{Inner radius (between tips). Must be non-negative and
no greater than \code{outer_radius}. Default \code{0.5}.}

\item{n}{Number of star points. Must be at least \code{2}. Default \code{5L}.}

\item{trans}{A \link{trans} object applied to the shape's computed points.
Default \code{\link[=trans_identity]{trans_identity()}} (no transform).}

\item{...}{Arguments passed to \code{\link[=style]{style()}}.}
}
\value{
A \link{drawable}.

For \code{shape_stars()}, a \link{sketch}.
}
\description{
\code{shape_star} is a \link{drawable} defined by a centroid and two radii: its
outline alternates \code{n} outer vertices (at \code{outer_radius}) with \code{n} inner
vertices (at \code{inner_radius}), evenly spaced by angle, giving the familiar
\code{n}-pointed star shape (e.g. \code{n = 5} a five-pointed star, \code{n = 6} a
Star-of-David-like hexagram outline).

\code{shape_stars()} is a vectorized version of \code{\link[=shape_star]{shape_star()}}: each argument
may be a vector, recycled against the others. The result is a \link{sketch}
containing one \code{shape_star()} per recycled row, rather than a single
drawable.
}
\details{
Like \code{\link[=shape_polygon]{shape_polygon()}}, \code{points} does not repeat its first vertex at the
end -- there are exactly \code{2 * n} distinct vertices, since
\code{grid::polygonGrob()} closes the path itself. The first vertex is an outer
one at angle \code{0}; use \code{trans_rotate()} to reorient the star, the same way
\code{\link[=shape_polygon]{shape_polygon()}}'s own low-\code{n} vertices are reoriented.

\code{inner_radius = outer_radius} degenerates the star into a regular
\code{2 * n}-gon (no points), and \code{inner_radius = 0} collapses every inner
vertex onto the centroid, giving a sharp \code{n}-pointed "asterisk" outline.

Recycling uses \code{purrr::pmap()}'s own vctrs-based rules: any length-1
element is broadcast to the common length; mismatched lengths greater
than 1 raise an error.
}
\examples{
draw(shape_star())
draw(shape_star(x = 1, y = 1, outer_radius = 0.5, n = 8, color = "darkred"))
draw(shape_star(n = 6, fill = "goldenrod"))

# a sharper star: inner_radius closer to 0
draw(shape_star(inner_radius = 0.2, fill = "steelblue"))

# rotating a star reorients its tips
draw(shape_star(n = 5, trans = trans_rotate(pi / 2)))

draw(shape_stars(x = 1:3, n = c(4L, 5L, 6L)))
draw(shape_stars(
  x = 0, y = 0, outer_radius = seq(3, 0.5, length.out = 6), n = 5,
  fill = rep(c("grey20", "grey90"), length.out = 6)
))

}
\seealso{
Other 2D shapes:
\code{\link[=shape_bezier]{shape_bezier()}},
\code{\link[=shape_blob]{shape_blob()}},
\code{\link[=shape_circle]{shape_circle()}},
\code{\link[=shape_combine]{shape_combine()}},
\code{\link[=shape_ellipse]{shape_ellipse()}},
\code{\link[=shape_polygon]{shape_polygon()}},
\code{\link[=shape_raw]{shape_raw()}},
\code{\link[=shape_rectangle]{shape_rectangle()}},
\code{\link[=shape_ribbon]{shape_ribbon()}},
\code{\link[=shape_stroke]{shape_stroke()}},
\code{\link[=shape_strokepath]{shape_strokepath()}},
\code{\link[=shape_twist]{shape_twist()}},
\code{\link[=shape_wedge]{shape_wedge()}}
}
\concept{2D shapes}
