% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trans.R
\name{trans_chain}
\alias{trans_chain}
\title{A sequence of composed transforms}
\usage{
trans_chain(steps = list())
}
\arguments{
\item{steps}{A list of \link{trans}/\link{trans_warp}/\code{trans_chain} objects.}
}
\description{
\code{trans_chain} is what \code{+} produces when combining transforms that can't
collapse into a single \link{trans} matrix -- e.g. a \link{trans_warp} with
another \link{trans_warp}, or a \link{trans_warp} mixed with a \link{trans}. It is not
usually constructed directly; it holds an ordered list of \code{steps}
(each a \link{trans} or \link{trans_warp}), applied in sequence -- \code{steps[[1]]}
first, \code{steps[[length(steps)]]} last -- exactly like chained \code{+} calls
on a \link{drawable} would suggest. Two consecutive \link{trans} (affine) steps
are \emph{not} automatically collapsed into one matrix when they're already
part of a chain (only a bare \code{trans + trans} collapses); this only
costs a little efficiency, not correctness.
}
\examples{
# combining an affine trans with a trans_warp produces a trans_chain,
# applied in the order given by +
chained <- trans_rotate(pi / 6) + trans_warp(amount = 0.08)
chained
draw(shape_rectangle(width = 1.5, height = 0.6, trans = chained))

}
\seealso{
Other transform helpers:
\code{\link[=trans]{trans()}},
\code{\link[=trans_affine]{trans_affine()}},
\code{\link[=trans_identity]{trans_identity()}},
\code{\link[=trans_reflect]{trans_reflect()}},
\code{\link[=trans_rotate]{trans_rotate()}},
\code{\link[=trans_scale]{trans_scale()}},
\code{\link[=trans_shear]{trans_shear()}},
\code{\link[=trans_translate]{trans_translate()}},
\code{\link[=trans_warp]{trans_warp()}}
}
\concept{transform helpers}
