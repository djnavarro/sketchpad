% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.R
\name{fill_scatter}
\alias{fill_scatter}
\title{Scattered-shape pattern fill}
\usage{
fill_scatter(
  unit = shape_circle(radius = 1),
  n = 6L,
  size = 0.2,
  spacing = 1,
  aspect = 1,
  seed = 1L,
  extend = "repeat"
)
}
\arguments{
\item{unit}{A small \link{drawable} to scatter copies of. Default
\code{shape_circle(radius = 1)}.}

\item{n}{Number of copies scattered per tile. Must be a positive
integer. Default \code{6L}.}

\item{size}{\code{unit}'s rescaled size, as a \code{"npc"} fraction of the tile.
Must be a number strictly between \code{0} and \code{1}. Default \code{0.2}.}

\item{spacing}{Tile size, as a fraction of the target's bounding box.
Must be a positive number. Default \code{1} (one tile spans the whole
shape, since \code{spacing < 1} risks the tiling distortion described
above).}

\item{aspect}{Width-to-height ratio of the target polygon's bounding
box. Must be a positive number. Default \code{1} (a square bounding box).}

\item{seed}{Integer seed for the scatter positions. Default \code{1L}.}

\item{extend}{Passed to \code{\link[grid:pattern]{grid::pattern()}}. Default \code{"repeat"}.}
}
\value{
A pattern object as returned by \code{\link[grid:pattern]{grid::pattern()}}, suitable for
use as the \code{fill} argument to \code{\link[grid:gpar]{grid::gpar()}}.
}
\description{
\code{fill_scatter()} generalizes \code{\link[=fill_stipple]{fill_stipple()}}: instead of a fixed dot,
it scatters copies of an arbitrary small \link{drawable} -- rendered with its
own \code{style} (colour, fill, linewidth), which may itself be another
\verb{fill_*()} pattern -- at random positions inside each tile, using
\code{\link[withr:with_seed]{withr::with_seed()}} for reproducibility exactly as \code{\link[=fill_stipple]{fill_stipple()}}
does.
}
\details{
\code{unit}'s own points are rescaled (preserving its own aspect ratio) to a
bounding box of size \code{size} and re-centred at each scattered position;
its absolute coordinates, position, and radius/width/etc. don't matter,
only its shape.

This needed two corrections neither \code{\link[=fill_stipple]{fill_stipple()}}'s circles nor
\code{\link[=fill_gradient]{fill_gradient()}}'s/\code{\link[=fill_checker]{fill_checker()}}'s rectangles did:
\itemize{
\item Every other \verb{fill_*()} helper's tile-squaring correction (\code{height = spacing * aspect}, keeping the tile physically square) was, by
itself, enough to keep circular/rectangular content correctly
proportioned. Arbitrary polygon content does not get the same
treatment: empirically, a \code{\link[grid:polygonGrob]{grid::polygonGrob()}} (or
\code{\link[grid:pathGrob]{grid::pathGrob()}}) used as pattern content renders as though it
inherits the \emph{target's own, uncorrected} bounding-box distortion
directly, regardless of the tile-squaring correction applied around
it -- confirmed by testing a hand-built circular polygon side by side
with an equivalent \code{\link[grid:circleGrob]{grid::circleGrob()}} in the same corrected tile:
the circle stayed circular, the polygon became an ellipse. So
\code{fill_scatter()} applies a second, explicit correction directly to
\code{unit}'s own vertex x-coordinates (dividing by \code{aspect}) on top of
the usual tile-squaring.
\item Repeated (tiled) polygon content can render with visible clipping
artifacts on Cairo devices -- confirmed interactively: a single
stamp, comfortably inside its tile's margins, rendered as a clean
shape when the tile spans the whole target (\code{spacing = 1}, so
\code{extend = "repeat"} is present but never actually exercised within
the visible, clipped area) but as a "bitten" partial shape once
\code{spacing < 1} made the device actually tile multiple copies. This
matches \code{\link[grid:pattern]{grid::pattern()}}'s own documented warning that "on Cairo
devices, use of clipping in the pattern definition should be avoided
because it is very likely to result in distortion of the pattern
tile." Circles/rectangles/rasters didn't show this in the rest of
the family, but arbitrary polygon geometry did. \code{spacing} therefore
defaults to \code{1} here (one tile spans the whole shape, scattering all
\code{n} copies across it at once) rather than the smaller, densely-tiled
defaults used elsewhere; setting \code{spacing < 1} is still possible for
a repeating scattered motif, but may show this distortion. (Later
testing on \code{\link[=fill_stipple]{fill_stipple()}} found the same \emph{actually-repeated tile
with multiple shapes} combination distorts circleGrob content too,
not just polygons -- see its "Known rendering risk" section. A
single tile with multiple shapes, as used by this function's
default, was never observed to have the problem; only real
repetition, \code{spacing < 1}, was.)
}
}
\seealso{
Other fill helpers:
\code{\link[=fill_checker]{fill_checker()}},
\code{\link[=fill_crosshatch]{fill_crosshatch()}},
\code{\link[=fill_flow]{fill_flow()}},
\code{\link[=fill_gradient]{fill_gradient()}},
\code{\link[=fill_halftone]{fill_halftone()}},
\code{\link[=fill_hatch]{fill_hatch()}},
\code{\link[=fill_image]{fill_image()}},
\code{\link[=fill_marble]{fill_marble()}},
\code{\link[=fill_noise]{fill_noise()}},
\code{\link[=fill_scribble]{fill_scribble()}},
\code{\link[=fill_solid]{fill_solid()}},
\code{\link[=fill_stipple]{fill_stipple()}},
\code{\link[=fill_stripe]{fill_stripe()}},
\code{\link[=fill_vignette]{fill_vignette()}}
}
\concept{fill helpers}
