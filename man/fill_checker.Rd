% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.R
\name{fill_checker}
\alias{fill_checker}
\title{Checkerboard pattern fill}
\usage{
fill_checker(
  color = c("black", "white"),
  spacing = 0.2,
  aspect = NULL,
  extend = "repeat"
)
}
\arguments{
\item{color}{Two or more checker colours. Default \code{c("black", "white")}.}

\item{spacing}{Baseline tile size, as a fraction of the target's bounding
box. Must be a positive number. Default \code{0.2}.}

\item{aspect}{Width-to-height ratio of the target polygon's bounding box.
Must be a positive number, or \code{NULL} (the default) to resolve it
automatically from the real target's own bounding-box aspect ratio at
\code{\link[=draw]{draw()}} time -- see the \link{fill} class. Passing a fixed number instead
computes the pattern once, immediately, against that value only.}

\item{extend}{Passed to \code{\link[grid:pattern]{grid::pattern()}}. Default \code{"repeat"}.}
}
\value{
A pattern object as returned by \code{\link[grid:pattern]{grid::pattern()}}, suitable for
use as the \code{fill} argument to \code{\link[grid:gpar]{grid::gpar()}}.
}
\description{
\code{fill_checker()} builds a \code{\link[grid:pattern]{grid::pattern()}} fill value that renders a
checkerboard, generalized from two colours to an arbitrary palette.
}
\details{
It's the cheapest member of the hatch family to build: a checkerboard
square has no direction the way a hatch line does (compare
\code{\link[=fill_hatch]{fill_hatch()}}'s corner-to-corner diagonal, needed specifically to tile
a \emph{sloped} line seamlessly), so the tile content here is just a grid of
plain quadrant rectangles -- the same two-colour-grid special case
\code{\link[=fill_crosshatch]{fill_crosshatch()}} already falls back to when \code{angle} is a multiple of
90 degrees, pulled out into its own helper.

The tile is subdivided into an \verb{n x n} grid, where \code{n = length(color)}.
The colour at grid cell \verb{(row, col)} (0-indexed) is \code{color[((row + col) \%\% n) + 1]} -- for the default two colours this reproduces the classic
2x2 checkerboard exactly; a longer \code{color} vector grows the grid rather
than adding a separate density argument, since a checkerboard's cell size
and colour count aren't independent concepts here.
}
\section{Known rendering risk with three or more colours}{
 The default
two-colour, four-rectangle tile has always rendered correctly, but
\code{color} vectors of length 3 or more (a 3x3 or larger grid of
rectangles) were found, at the default \code{spacing}, to trigger the same
upstream \code{grid}/Cairo issue documented at \code{\link[=fill_stipple]{fill_stipple()}}'s "Known
rendering risk" section -- several shapes inside a genuinely \emph{repeated}
\code{\link[grid:pattern]{grid::pattern()}} tile can render distorted (here, collapsing to a
single solid colour instead of a grid), even though the same tile
content renders correctly as a single, non-repeated tile (\code{spacing = 1}). \strong{Visually check rendered output} before relying on more than
two \code{color}s for anything beyond casual use.

As with the other \verb{fill_*()} helpers, \code{\link[grid:pattern]{grid::pattern()}} tiles are sized
as a fraction of the target polygon's own bounding box rather than a
fixed physical square, so the checker squares would render as
rectangles, not squares, on a non-square bounding box -- the same
tile-squaring technique \code{\link[=fill_stipple]{fill_stipple()}} uses for its dots. \code{aspect}
resolves this automatically by default (see \code{\link[=fill_hatch]{fill_hatch()}}'s own
\code{aspect} docs).
}

\examples{
draw(shape_circle(fill = fill_checker(color = c("black", "white"))))

# a coarser, differently-coloured checkerboard
draw(shape_circle(
  fill = fill_checker(color = c("steelblue", "white"), spacing = 0.4)
))

# three or more colours grow the grid rather than alternating just two;
# spacing = 1 avoids the tile-repetition rendering risk noted above
draw(shape_circle(
  fill = fill_checker(color = c("steelblue", "white", "tomato"), spacing = 1)
))

}
\seealso{
Other fill helpers:
\code{\link[=fill]{fill()}},
\code{\link[=fill_charcoal]{fill_charcoal()}},
\code{\link[=fill_crosshatch]{fill_crosshatch()}},
\code{\link[=fill_flow]{fill_flow()}},
\code{\link[=fill_gradient]{fill_gradient()}},
\code{\link[=fill_halftone]{fill_halftone()}},
\code{\link[=fill_hatch]{fill_hatch()}},
\code{\link[=fill_image]{fill_image()}},
\code{\link[=fill_marble]{fill_marble()}},
\code{\link[=fill_noise]{fill_noise()}},
\code{\link[=fill_none]{fill_none()}},
\code{\link[=fill_scatter]{fill_scatter()}},
\code{\link[=fill_scribble]{fill_scribble()}},
\code{\link[=fill_solid]{fill_solid()}},
\code{\link[=fill_stipple]{fill_stipple()}},
\code{\link[=fill_stripe]{fill_stripe()}},
\code{\link[=fill_vignette]{fill_vignette()}}
}
\concept{fill helpers}
