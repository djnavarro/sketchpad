% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curve_spiral.R
\name{curve_spiral}
\alias{curve_spiral}
\alias{curve_spirals}
\title{An open spiral}
\usage{
curve_spiral(
  x = 0,
  y = 0,
  radius_start = 0,
  radius_end = 1,
  turns = 3,
  n = 200L,
  trans = trans_identity(),
  ...
)

curve_spirals(
  x = 0,
  y = 0,
  radius_start = 0,
  radius_end = 1,
  turns = 3,
  n = 200L,
  trans = trans_identity(),
  ...
)
}
\arguments{
\item{x, y}{Centroid coordinates. Default \code{0}.}

\item{radius_start, radius_end}{Radius at the start/end of the path. Must
be non-negative. Default \code{0}/\code{1}.}

\item{turns}{Number of full revolutions. Must be positive. Default \code{3}.}

\item{n}{Number of points used to approximate the spiral. Default \code{200L}.}

\item{trans}{A \link{trans} object applied to the curve's computed points.
Default \code{\link[=trans_identity]{trans_identity()}} (no transform).}

\item{...}{Arguments passed to \code{\link[=style]{style()}}.}
}
\value{
A \link{drawable}.

For \code{curve_spirals()}, a \link{sketch}.
}
\description{
\code{curve_spiral} is a \link{drawable} whose path winds around a centroid
\verb{(x, y)}, sweeping through \code{turns} full revolutions while its radius
interpolates linearly from \code{radius_start} to \code{radius_end}. With
\code{radius_start = radius_end} this traces a circle repeated \code{turns} times
(visually indistinguishable from a single circle, since the path
retraces itself); the usual case has \code{radius_start != radius_end}, giving
an Archimedean-style spiral that grows or shrinks outward.

\code{curve_spirals()} is a vectorized version of \code{\link[=curve_spiral]{curve_spiral()}}: each
argument may be a vector, recycled against the others via
\code{purrr::pmap()}'s own vctrs-based rules (any length-1 element is
broadcast to the common length; mismatched lengths greater than 1
raise an error). The result is a \link{sketch} containing one
\code{curve_spiral()} per recycled row, rather than a single drawable.
}
\details{
\code{style@fill} has no effect for \code{curve_spiral()} -- see \link{drawable}'s
\code{geometry} documentation for why \code{"path"} geometries have no interior to
fill. Passing \code{fill} via \code{...} is still accepted (it's simply ignored at
draw time), since \code{style()} is shared across every \code{geometry}.
}
\examples{
draw(curve_spiral(radius_start = 0, radius_end = 1, turns = 4))

# radius_start > radius_end spirals inward instead of outward
draw(curve_spiral(
  radius_start = 1, radius_end = 0.1, turns = 5, linewidth = 2
))

# equal start/end radii retrace a circle -- rarely useful on its own,
# but shows turns has no effect on shape when radius doesn't change
draw(curve_spiral(radius_start = 1, radius_end = 1, turns = 3))

draw(curve_spirals(x = c(0, 3, 6), turns = c(2, 3, 4)))

# nested spirals sharing a centroid, growing radius each time
draw(curve_spirals(
  radius_start = seq(0.1, 0.5, length.out = 4),
  radius_end = seq(0.6, 1, length.out = 4)
))

}
\seealso{
Other 1D curves:
\code{\link[=curve_arc]{curve_arc()}},
\code{\link[=curve_bezier]{curve_bezier()}},
\code{\link[=curve_line]{curve_line()}},
\code{\link[=curve_raw]{curve_raw()}},
\code{\link[=curve_scribble]{curve_scribble()}},
\code{\link[=curve_twist]{curve_twist()}}
}
\concept{1D curves}
