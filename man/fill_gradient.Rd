% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.R
\name{fill_gradient}
\alias{fill_gradient}
\title{Gradient fill}
\usage{
fill_gradient(
  colors = c("white", "black"),
  type = c("linear", "radial"),
  angle = 45,
  stops = NULL,
  spacing = 1,
  aspect = 1,
  extend = "pad"
)
}
\arguments{
\item{colors}{Two or more colours to interpolate between.}

\item{type}{Either \code{"linear"} or \code{"radial"}. Default \code{"linear"}.}

\item{angle}{Gradient direction in degrees, for \code{type = "linear"} only
(ignored for \code{"radial"}). Default \code{45}.}

\item{stops}{Colour stop positions, as a numeric vector the same length
as \code{colors}, or \code{NULL} to space them evenly (the default used by
\code{\link[grid:linearGradient]{grid::linearGradient()}}/\code{\link[grid:radialGradient]{grid::radialGradient()}}). Default \code{NULL}.}

\item{spacing}{Tile size, as a fraction of the target's bounding box.
Must be a positive number. Default \code{1} (one tile spans the whole
shape).}

\item{aspect}{Width-to-height ratio of the target polygon's bounding box.
Must be a positive number. Default \code{1} (a square bounding box).}

\item{extend}{Passed to the inner \code{\link[grid:linearGradient]{grid::linearGradient()}}/
\code{\link[grid:radialGradient]{grid::radialGradient()}}, controlling what happens beyond the colour
stops. Default \code{"pad"}.}
}
\value{
A pattern object as returned by \code{\link[grid:pattern]{grid::pattern()}}, suitable for
use as the \code{fill} argument to \code{\link[grid:gpar]{grid::gpar()}}.
}
\description{
\code{fill_gradient()} builds a \code{\link[grid:pattern]{grid::pattern()}} fill value whose tile content
is a single rectangle filled with a \code{\link[grid:linearGradient]{grid::linearGradient()}} or
\code{\link[grid:radialGradient]{grid::radialGradient()}}, depending on \code{type}.
}
\details{
Like the other \verb{fill_*()} helpers, this needs the target's bounding-box
aspect ratio (\code{aspect}) to render true rather than stretched -- but the
correction is applied differently here. Rather than adjusting the
gradient's own coordinates (the way \code{\link[=fill_hatch]{fill_hatch()}} adjusts a segment's
direction), \code{fill_gradient()} corrects the \emph{tile} itself to be physically
square, exactly as \code{\link[=fill_stipple]{fill_stipple()}} does for its dots: once the tile is
square, a gradient specified inside it in plain \code{"npc"} needs no further
correction to render at the requested \code{angle}, or as a true circle for
\code{type = "radial"}.

This also means a gradient tile has none of \code{\link[=fill_hatch]{fill_hatch()}}'s periodicity
concerns: adjacent tiles are simply identical copies of the same square
gradient, with nothing analogous to a hatch line's tile-edge dashing to
avoid.

With the default \code{spacing = 1}, one tile spans (and, for a non-square
bounding box, slightly overshoots) the target's entire bounding box,
giving a single smooth gradient across the whole shape -- the overshoot
is invisibly clipped away by the target's own outline. Set \code{spacing < 1}
for a repeating pattern of small gradient motifs instead.
}
\seealso{
Other fill helpers:
\code{\link[=fill_checker]{fill_checker()}},
\code{\link[=fill_crosshatch]{fill_crosshatch()}},
\code{\link[=fill_flow]{fill_flow()}},
\code{\link[=fill_halftone]{fill_halftone()}},
\code{\link[=fill_hatch]{fill_hatch()}},
\code{\link[=fill_image]{fill_image()}},
\code{\link[=fill_marble]{fill_marble()}},
\code{\link[=fill_noise]{fill_noise()}},
\code{\link[=fill_scatter]{fill_scatter()}},
\code{\link[=fill_scribble]{fill_scribble()}},
\code{\link[=fill_solid]{fill_solid()}},
\code{\link[=fill_stipple]{fill_stipple()}},
\code{\link[=fill_stripe]{fill_stripe()}},
\code{\link[=fill_vignette]{fill_vignette()}}
}
\concept{fill helpers}
