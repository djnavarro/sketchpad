% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.R
\name{fill_hatch}
\alias{fill_hatch}
\title{Diagonal hatch pattern fill}
\usage{
fill_hatch(
  angle = 45,
  spacing = 0.1,
  aspect = 1,
  color = "black",
  linewidth = 1,
  extend = "repeat"
)
}
\arguments{
\item{angle}{Hatch angle in degrees, measured counterclockwise from the
positive x-axis. Default \code{45}.}

\item{spacing}{Baseline tile size, as a fraction of the target's bounding
box. Must be a positive number. Default \code{0.1}.}

\item{aspect}{Width-to-height ratio of the target polygon's bounding box.
Must be a positive number. Default \code{1} (a square bounding box).}

\item{color}{Line colour. Default \code{"black"}.}

\item{linewidth}{Line width. Default \code{1}.}

\item{extend}{Passed to \code{\link[grid:pattern]{grid::pattern()}}. Default \code{"repeat"}.}
}
\value{
A pattern object as returned by \code{\link[grid:pattern]{grid::pattern()}}, suitable for
use as the \code{fill} argument to \code{\link[grid:gpar]{grid::gpar()}}.
}
\description{
\code{fill_hatch()} builds a \code{\link[grid:pattern]{grid::pattern()}} fill value that renders a
repeating diagonal hatch line. It's meant to be used as the \code{fill}
argument to \code{\link[grid:gpar]{grid::gpar()}} (and eventually \code{style()}'s \code{fill} property),
in place of a plain colour.
}
\details{
\code{\link[grid:pattern]{grid::pattern()}} tiles are sized as a \emph{fraction of the target polygon's
own bounding box}, not a fixed physical square, so a tile that looks
square in that relative sense can be a stretched rectangle in absolute
terms whenever the target's bounding box isn't square itself -- which
distorts any angle baked directly into the pattern content. Pass the
target's bounding-box aspect ratio (width / height) as \code{aspect} to
correct for this; the default \code{aspect = 1} is only exact for a square
bounding box.

Internally, the hatch line is always drawn as a plain diagonal from one
tile corner to the opposite corner (or the mirror image, for a
negative-sloped angle) -- never at an arbitrary slope baked into the
segment's own coordinates. A corner-to-corner diagonal is the only slope
that tiles seamlessly under \code{\link[grid:pattern]{grid::pattern()}}'s \code{extend = "repeat"},
which translates tile copies by whole tile-widths/heights only; any other
local slope leaves a visible mismatch ("dashing") at every tile edge. The
desired angle is instead achieved entirely by choosing the tile's
\code{width}/\code{height} ratio. Exactly horizontal/vertical angles are handled as
a special case, since a straight (non-diagonal) line tiles seamlessly at
any tile aspect ratio.
}
\seealso{
Other fill helpers:
\code{\link[=fill_crosshatch]{fill_crosshatch()}},
\code{\link[=fill_noise]{fill_noise()}},
\code{\link[=fill_solid]{fill_solid()}},
\code{\link[=fill_stipple]{fill_stipple()}}
}
\concept{fill helpers}
