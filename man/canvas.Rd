% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canvas.R
\name{canvas}
\alias{canvas}
\title{Canvas settings for a sketch}
\usage{
canvas(background = NA_character_, xlim = NULL, ylim = NULL, clip = FALSE)
}
\arguments{
\item{background}{Background fill, drawn beneath every shape in the
sketch. Either a plain colour string, or the output of a \verb{fill_*()}
helper -- see \code{\link[=style]{style()}}'s \code{fill} argument for the full family. Default
\code{\link[=fill_none]{fill_none()}} (no background drawn; the page's own background shows
through, matching \code{draw()}'s behavior before \code{canvas} existed).}

\item{xlim, ylim}{Fixed axis limits, each a numeric vector of length 2, or
\code{NULL} (the default) to compute them from the sketch's own shapes at
draw time, as \code{\link[=draw]{draw()}} already did before \code{canvas} existed. An explicit
\code{xlim}/\code{ylim} passed to \code{\link[=draw]{draw()}} itself always takes precedence over
these.}

\item{clip}{Whether to clip content to \code{xlim}/\code{ylim}. Must be a single
logical. Default \code{FALSE} -- see Details.}
}
\description{
\code{canvas} bundles the settings \code{\link[=draw]{draw()}} applies to a \link{sketch} as a whole,
before drawing any of its shapes: a \code{background} fill and an optional
fixed \code{xlim}/\code{ylim} frame. It plays the same role for \code{sketch} that
\code{\link[=style]{style()}} plays for a single \link{drawable} -- a small, reusable settings
object, validated independently and stored as a property (\code{sketch}'s own
\code{canvas}) rather than a flat list of arguments.
}
\details{
\code{xlim}/\code{ylim} only fix the coordinate \emph{scale} \code{\link[=draw]{draw()}} maps onto the
page -- they do not, by themselves, clip content that falls outside that
range; a shape wider than its sketch's \code{canvas} still renders in full,
spilling past the frame, exactly as passing \code{xlim}/\code{ylim} to \code{\link[=draw]{draw()}}
directly already behaves. This is a deliberate default: shapes built
from a \link{noise_field}/\link{noise_bridge} (e.g. \code{\link[=shape_blob]{shape_blob()}}, \code{\link[=shape_twist]{shape_twist()}})
can have somewhat unpredictable extents, and silently cropping part of
one away is a worse failure mode than a visibly overflowing shape, which
is an obvious cue to adjust the sketch's parameters. Set \code{clip = TRUE} to
opt into hard clipping at \code{xlim}/\code{ylim} instead -- most useful once
\code{background} is also set to something other than \code{\link[=fill_none]{fill_none()}}, since an
opaque background otherwise has visible content bleeding past its own
edge onto the bare page. \code{clip} has no visible effect when \code{xlim}/\code{ylim}
are both left \code{NULL}, since \code{\link[=draw]{draw()}} then computes them from the
sketch's own shapes, which by construction never exceed that range.
}
\examples{
canvas()
canvas(background = "grey90")

# a background is drawn beneath every shape in the sketch
draw(sketch(canvas = canvas(background = "grey90")) + shape_blob(radius = 1))

# xlim/ylim alone only fix the coordinate scale -- a shape wider than the
# frame still overflows it
overflowing <- sketch(canvas = canvas(
  background = "white", xlim = c(-1, 1), ylim = c(-1, 1)
)) + shape_circle(radius = 1.4)
draw(overflowing)

# clip = TRUE hard-clips content at xlim/ylim instead
overflowing@canvas@clip <- TRUE
draw(overflowing)

}
\seealso{
Other core structure:
\code{\link[=draw]{draw()}},
\code{\link[=drawable]{drawable()}},
\code{\link[=sketch]{sketch()}},
\code{\link[=style]{style()}},
\code{\link[=xy]{xy()}}
}
\concept{core structure}
