% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.R
\name{fill_image}
\alias{fill_image}
\title{User-supplied raster image fill}
\usage{
fill_image(
  image,
  preserve_aspect = TRUE,
  spacing = 1,
  aspect = 1,
  interpolate = TRUE,
  extend = "repeat"
)
}
\arguments{
\item{image}{A raster image: a \code{"raster"} object, or a matrix/array
accepted by \code{\link[grDevices:as.raster]{grDevices::as.raster()}}. Not a file path -- read the file
first with e.g. \code{png::readPNG()}, \code{jpeg::readJPEG()}, or
\code{magick::image_read()}.}

\item{preserve_aspect}{Whether to letterbox the image to preserve its own
pixel aspect ratio (\code{TRUE}), or stretch it to exactly fill each tile
(\code{FALSE}). Default \code{TRUE}.}

\item{spacing}{Tile size, as a fraction of the target's bounding box.
Must be a positive number. Default \code{1} (one tile spans the whole
shape).}

\item{aspect}{Width-to-height ratio of the target polygon's bounding box.
Must be a positive number. Default \code{1} (a square bounding box).}

\item{interpolate}{Passed to \code{\link[grid:rasterGrob]{grid::rasterGrob()}}. Default \code{TRUE}.}

\item{extend}{Passed to \code{\link[grid:pattern]{grid::pattern()}}. Default \code{"repeat"}.}
}
\value{
A pattern object as returned by \code{\link[grid:pattern]{grid::pattern()}}, suitable for
use as the \code{fill} argument to \code{\link[grid:gpar]{grid::gpar()}}.
}
\description{
\code{fill_image()} builds a \code{\link[grid:pattern]{grid::pattern()}} fill value whose tile content is
an arbitrary bitmap, rather than a texture generated by this package. It
plugs any raster the caller already has in memory into the same
\code{grid::pattern()} machinery \code{\link[=fill_noise]{fill_noise()}} uses for its own generated
raster -- but with the raster supplied, not computed.
}
\details{
\code{fill_image()} doesn't read image files itself, to avoid adding an image
I/O dependency (\code{png}/\code{jpeg}/\code{magick}, ...) to a package that otherwise
has none. Load the file first with whichever of those packages is
already on hand, then pass the result (or \code{\link[grDevices:as.raster]{grDevices::as.raster()}} of
it) as \code{image} -- e.g. \code{fill_image(png::readPNG("logo.png"))} or
\code{fill_image(magick::image_read("logo.png"))}. Anything
\code{\link[grDevices:as.raster]{grDevices::as.raster()}} accepts works: a \code{"raster"} object, a character
matrix of colour strings, or a numeric array of \code{0}-\code{1} RGB/RGBA
intensities.

Unlike the other \verb{fill_*()} helpers, \code{fill_image()}'s content has its
\emph{own} pixel aspect ratio to account for, on top of the usual
target-bounding-box correction every helper needs (\code{aspect}, corrected
for exactly as \code{\link[=fill_noise]{fill_noise()}} does -- keeping the tile physically
square). With \code{preserve_aspect = TRUE} (the default), the image is
letterboxed to fit that square tile without distorting it: whichever of
width/height is smaller in the image's own pixel dimensions is shrunk
to fit, leaving the tile's remaining margin transparent. Set
\code{preserve_aspect = FALSE} to stretch the image to fill the tile exactly
instead, matching \code{\link[=fill_noise]{fill_noise()}}'s own (always-stretched) behaviour.
}
\examples{
img <- matrix(c("red", "white", "white", "blue"), nrow = 2)
draw(shape_circle(fill = fill_image(img, preserve_aspect = FALSE)))

# a non-square image, letterboxed (default) vs. stretched to fill the tile
wide_img <- matrix(c("red", "white", "blue"), nrow = 1)
draw(shape_rectangle(width = 2, height = 1, fill = fill_image(wide_img)))
draw(shape_rectangle(
  width = 2,
  height = 1,
  fill = fill_image(wide_img, preserve_aspect = FALSE)
))

}
\seealso{
Other fill helpers:
\code{\link[=fill_charcoal]{fill_charcoal()}},
\code{\link[=fill_checker]{fill_checker()}},
\code{\link[=fill_crosshatch]{fill_crosshatch()}},
\code{\link[=fill_flow]{fill_flow()}},
\code{\link[=fill_gradient]{fill_gradient()}},
\code{\link[=fill_halftone]{fill_halftone()}},
\code{\link[=fill_hatch]{fill_hatch()}},
\code{\link[=fill_marble]{fill_marble()}},
\code{\link[=fill_noise]{fill_noise()}},
\code{\link[=fill_none]{fill_none()}},
\code{\link[=fill_scatter]{fill_scatter()}},
\code{\link[=fill_scribble]{fill_scribble()}},
\code{\link[=fill_solid]{fill_solid()}},
\code{\link[=fill_stipple]{fill_stipple()}},
\code{\link[=fill_stripe]{fill_stripe()}},
\code{\link[=fill_vignette]{fill_vignette()}}
}
\concept{fill helpers}
