% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_ellipse.R
\name{shape_ellipse}
\alias{shape_ellipse}
\alias{shape_ellipses}
\title{An ellipse}
\usage{
shape_ellipse(
  x = 0,
  y = 0,
  x_radius = 1,
  y_radius = 1,
  n = 100L,
  trans = trans_identity(),
  ...
)

shape_ellipses(
  x = 0,
  y = 0,
  x_radius = 1,
  y_radius = 1,
  n = 100L,
  trans = trans_identity(),
  ...
)
}
\arguments{
\item{x, y}{Centroid coordinates. Default \code{0}.}

\item{x_radius, y_radius}{Radii along the x and y axes. Must be
non-negative. Default \code{1}.}

\item{n}{Number of points used to approximate the ellipse. Default \code{100L}.}

\item{trans}{A \link{trans} object applied to the shape's computed points.
Default \code{\link[=trans_identity]{trans_identity()}} (no transform).}

\item{...}{Arguments passed to \code{\link[=style]{style()}}.}
}
\value{
A \link{drawable}.

For \code{shape_ellipses()}, a \link{sketch}.
}
\description{
\code{shape_ellipse} is a \link{drawable} defined by a centroid and independent x/y
radii; its vertices are computed as evenly spaced points around the
circumference, generalizing \code{\link[=shape_circle]{shape_circle()}} (which is a \code{shape_ellipse}
with equal radii in both directions).

\code{shape_ellipses()} is a vectorized version of \code{\link[=shape_ellipse]{shape_ellipse()}}: each
argument may be a vector, recycled against the others. The result is a
\link{sketch} containing one \code{shape_ellipse()} per recycled row, rather than
a single drawable.
}
\details{
Recycling uses \code{purrr::pmap()}'s own vctrs-based rules: any length-1
element is broadcast to the common length; mismatched lengths greater
than 1 raise an error.
}
\examples{
draw(shape_ellipse(x_radius = 2, y_radius = 1))
draw(shape_ellipse(x = 1, y = 1, x_radius = 0.5, y_radius = 1, n = 6L, color = "darkred"))
draw(shape_ellipse(x_radius = 2, y_radius = 1, fill = fill_gradient(angle = 90)))

# rotating an ellipse (rather than swapping its radii) tilts its axes
draw(shape_ellipse(x_radius = 2, y_radius = 0.7, trans = trans_rotate(pi / 6)))

draw(shape_ellipses(x = 1:3, x_radius = c(0.5, 1, 1.5), y_radius = 0.5))
draw(shape_ellipses(
  x = 0, y = 0, x_radius = 1, y_radius = 0.3,
  trans = purrr::map(seq(0, pi, length.out = 6), trans_rotate)
))

}
\seealso{
Other 2D shapes:
\code{\link[=shape_bezier]{shape_bezier()}},
\code{\link[=shape_blob]{shape_blob()}},
\code{\link[=shape_circle]{shape_circle()}},
\code{\link[=shape_polygon]{shape_polygon()}},
\code{\link[=shape_raw]{shape_raw()}},
\code{\link[=shape_rectangle]{shape_rectangle()}},
\code{\link[=shape_ribbon]{shape_ribbon()}},
\code{\link[=shape_ribbonpath]{shape_ribbonpath()}},
\code{\link[=shape_stroke]{shape_stroke()}},
\code{\link[=shape_twist]{shape_twist()}},
\code{\link[=shape_wedge]{shape_wedge()}}
}
\concept{2D shapes}
