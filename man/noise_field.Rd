% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/noise_field.R
\name{noise_field}
\alias{noise_field}
\title{A sampled scalar noise field}
\usage{
noise_field(
  noise = ambient::gen_simplex,
  fractal = ambient::fbm,
  frequency = 1,
  octaves = 2L,
  seed = 1L
)
}
\arguments{
\item{noise}{A noise-generating function, passed to
\code{\link[ambient:fracture]{ambient::fracture()}}'s \code{noise} argument (e.g. \code{\link[ambient:gen_simplex]{ambient::gen_simplex()}},
\code{\link[ambient:gen_perlin]{ambient::gen_perlin()}}, \code{\link[ambient:gen_worley]{ambient::gen_worley()}}). Default
\code{\link[ambient:gen_simplex]{ambient::gen_simplex()}}.}

\item{fractal}{A fractal combinator function, passed to
\code{\link[ambient:fracture]{ambient::fracture()}}'s \code{fractal} argument (e.g. \code{\link[ambient:fbm]{ambient::fbm()}},
\code{\link[ambient:billow]{ambient::billow()}}). Default \code{\link[ambient:fbm]{ambient::fbm()}}.}

\item{frequency}{Noise frequency. Must be non-negative. Default \code{1}.}

\item{octaves}{Number of noise octaves. Must be a positive integer.
Default \code{2L}.}

\item{seed}{Integer seed for the noise field. Default \code{1L}.}
}
\description{
\code{noise_field} bundles the settings needed to sample a scalar-valued
noise field from \pkg{ambient} at arbitrary \verb{(x, y)} positions: which
base noise function and fractal combinator to use
(\code{\link[ambient:fracture]{ambient::fracture()}}'s \code{noise}/\code{fractal} arguments), how coarse or
fine the field is (\code{frequency}), how many octaves of detail to layer
(\code{octaves}), and which \code{seed} to draw from. \code{\link[=noise_sample]{noise_sample()}} evaluates
a \code{noise_field} at a set of positions and rescales the result into a
target range -- the operation shared by \code{\link[=shape_blob]{shape_blob()}}'s radius
perturbation and \code{\link[=shape_ribbon]{shape_ribbon()}}/\code{\link[=shape_twist]{shape_twist()}}'s width modulation,
factored out here so it isn't duplicated across those three
constructors, and so the noise/fractal functions themselves are
configurable rather than hardcoded to \code{\link[ambient:gen_simplex]{ambient::gen_simplex()}}/
\code{\link[ambient:fbm]{ambient::fbm()}}.
}
\examples{
noise_field(frequency = 2, octaves = 3L, seed = 4821L)

# a noise_field's effect is easiest to see through a drawable that
# samples it -- lower frequency gives broad, gentle undulation, higher
# frequency a bumpier, more textured one
draw(shape_blob(radius = 1, range = 0.4, distortion = noise_field(frequency = 0.5, seed = 4821L)))
draw(shape_blob(radius = 1, range = 0.4, distortion = noise_field(frequency = 6, seed = 4821L)))

# more octaves layer finer detail on top of the base frequency
draw(shape_blob(radius = 1, range = 0.4, distortion = noise_field(octaves = 6L, seed = 4821L)))

}
\seealso{
Other noise helpers:
\code{\link[=noise_bridge]{noise_bridge()}},
\code{\link[=noise_sample]{noise_sample()}}
}
\concept{noise helpers}
