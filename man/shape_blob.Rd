% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_blob.R
\name{shape_blob}
\alias{shape_blob}
\alias{shape_blobs}
\title{An irregular, "blobby" circle}
\usage{
shape_blob(
  x = 0,
  y = 0,
  radius = 1,
  range = 0.2,
  n = 100L,
  distortion = noise_field(),
  trans = trans_identity(),
  ...
)

shape_blobs(
  x = 0,
  y = 0,
  radius = 1,
  range = 0.2,
  n = 100L,
  distortion = noise_field(),
  trans = trans_identity(),
  ...
)
}
\arguments{
\item{x, y}{Centroid coordinates. Default \code{0}.}

\item{radius}{Mean radius. Must be non-negative. Default \code{1}.}

\item{range}{Amplitude of the radius distortion. Must be non-negative.
Default \code{0.2}.}

\item{n}{Number of points used to approximate the outline. Default \code{100L}.}

\item{distortion}{A \link{noise_field} controlling the radius distortion.
Default \code{noise_field()}.}

\item{trans}{A \link{trans} object applied to the shape's computed points.
Default \code{\link[=trans_identity]{trans_identity()}} (no transform).}

\item{...}{Arguments passed to \code{\link[=style]{style()}}.}
}
\value{
A \link{drawable}.

For \code{shape_blobs()}, a \link{sketch}.
}
\description{
\code{shape_blob} is a \link{drawable} similar to \link{shape_circle}, except that its
radius varies smoothly around the circumference according to
Perlin/simplex noise generated with \pkg{ambient}.

\code{shape_blobs()} is a vectorized version of \code{\link[=shape_blob]{shape_blob()}}: each
argument may be a vector, recycled against the others via
\code{purrr::pmap()}'s own vctrs-based rules. The result is a \link{sketch}
containing one \code{shape_blob()} per recycled row, rather than a single
drawable.
}
\details{
Any length-1 element is broadcast to the common length; mismatched
lengths greater than 1 raise an error. A shared \code{distortion}
\link{noise_field} is automatically recycled across every blob; pass a
\code{list()} of several different \code{noise_field}s instead to vary it per
blob.
}
\examples{
draw(shape_blob(
  radius = 1,
  range = 0.3,
  distortion = noise_field(seed = 4821)
))

# a larger range or lower frequency distortion gives a more irregular
# outline; a higher frequency gives a bumpier, lumpier one
draw(shape_blob(
  radius = 1,
  range = 0.6,
  distortion = noise_field(seed = 4821)
))
draw(shape_blob(
  radius = 1, range = 0.3,
  distortion = noise_field(frequency = 5, seed = 4821)
))

draw(shape_blob(
  radius = 1,
  range = 0.3,
  fill = fill_charcoal(),
  color = NA_character_
))

draw(shape_blobs(x = 1:3, radius = c(0.5, 1, 1.5), range = 0.2))

# a shared distortion recycles across every blob; a list() of several
# different noise_field()s varies it per blob instead
draw(shape_blobs(
  x = 1:3, radius = 0.8, range = 0.3,
  distortion = list(
    noise_field(seed = 1),
    noise_field(seed = 2),
    noise_field(seed = 3)
  )
))

}
\seealso{
Other 2D shapes:
\code{\link[=shape_bezier]{shape_bezier()}},
\code{\link[=shape_circle]{shape_circle()}},
\code{\link[=shape_ellipse]{shape_ellipse()}},
\code{\link[=shape_polygon]{shape_polygon()}},
\code{\link[=shape_raw]{shape_raw()}},
\code{\link[=shape_rectangle]{shape_rectangle()}},
\code{\link[=shape_ribbon]{shape_ribbon()}},
\code{\link[=shape_stroke]{shape_stroke()}},
\code{\link[=shape_strokepath]{shape_strokepath()}},
\code{\link[=shape_twist]{shape_twist()}},
\code{\link[=shape_wedge]{shape_wedge()}}
}
\concept{2D shapes}
