% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_wedge.R
\name{shape_wedge}
\alias{shape_wedge}
\alias{shape_wedges}
\title{A pie-slice wedge}
\usage{
shape_wedge(
  x = 0,
  y = 0,
  radius = 1,
  start = 0,
  end = pi/2,
  n = 100L,
  trans = trans_identity(),
  ...
)

shape_wedges(
  x = 0,
  y = 0,
  radius = 1,
  start = 0,
  end = pi/2,
  n = 100L,
  trans = trans_identity(),
  ...
)
}
\arguments{
\item{x, y}{Centroid coordinates. Default \code{0}.}

\item{radius}{Radius. Must be non-negative. Default \code{1}.}

\item{start, end}{Start/end angle of the arc, in radians. Default \code{0}/
\code{pi / 2}.}

\item{n}{Number of points used to approximate the arc. Must be at least
\code{2}. Default \code{100L}.}

\item{trans}{A \link{trans} object applied to the shape's computed points.
Default \code{\link[=trans_identity]{trans_identity()}} (no transform).}

\item{...}{Arguments passed to \code{\link[=style]{style()}}.}
}
\value{
A \link{drawable}.

For \code{shape_wedges()}, a \link{sketch}.
}
\description{
\code{shape_wedge} is a \link{drawable} defined by a centroid, a radius, and a
\code{start}/\code{end} angle (in radians): its outline is the centroid, followed by
\code{n} points along the circular arc from \code{start} to \code{end}.

\code{shape_wedges()} is a vectorized version of \code{\link[=shape_wedge]{shape_wedge()}}: each
argument may be a vector, recycled against the others. The result is a
\link{sketch} containing one \code{shape_wedge()} per recycled row, rather than a
single drawable.
}
\details{
\code{grid}'s own polygon closing then draws the final straight edge back from
the arc's last point to the centroid, giving the familiar pie-slice/wedge
shape. \code{\link[=curve_arc]{curve_arc()}} is the arc alone, with no centroid vertex or fill.

Recycling uses \code{purrr::pmap()}'s own vctrs-based rules: any length-1
element is broadcast to the common length; mismatched lengths greater
than 1 raise an error.
}
\examples{
draw(shape_wedge(start = 0, end = pi / 2))
draw(shape_wedge(x = 1, y = 1, radius = 0.5, start = pi, end = 2 * pi, color = "darkred"))

# a nearly-full sweep gives a pac-man-like shape; the arc always closes
# straight back to the centroid
draw(shape_wedge(start = 0, end = 1.9 * pi, fill = "goldenrod"))

draw(shape_wedges(start = 0, end = seq(pi / 2, 2 * pi, length.out = 3)))

# a pie chart: adjacent wedges sharing a centroid, one slice per value
value <- c(30, 20, 50)
cum <- c(0, cumsum(value)) / sum(value) * 2 * pi
draw(shape_wedges(
  start = cum[-length(cum)], end = cum[-1],
  fill = c("steelblue", "tomato", "goldenrod")
))

}
\seealso{
Other 2D shapes:
\code{\link[=shape_bezier]{shape_bezier()}},
\code{\link[=shape_blob]{shape_blob()}},
\code{\link[=shape_circle]{shape_circle()}},
\code{\link[=shape_ellipse]{shape_ellipse()}},
\code{\link[=shape_polygon]{shape_polygon()}},
\code{\link[=shape_raw]{shape_raw()}},
\code{\link[=shape_rectangle]{shape_rectangle()}},
\code{\link[=shape_ribbon]{shape_ribbon()}},
\code{\link[=shape_ribbonpath]{shape_ribbonpath()}},
\code{\link[=shape_stroke]{shape_stroke()}},
\code{\link[=shape_twist]{shape_twist()}}
}
\concept{2D shapes}
