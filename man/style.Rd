% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style.R
\name{style}
\alias{style}
\title{Graphical style for a drawable object}
\usage{
style(
  color = "black",
  fill = "black",
  linewidth = 1,
  linetype = "solid",
  linejoin = "round",
  lineend = "round",
  linemitre = 10,
  rule = "evenodd",
  color_alpha = 1,
  fill_alpha = 1
)
}
\arguments{
\item{color}{Stroke colour. Default \code{"black"}.}

\item{fill}{Fill colour or pattern. Either a plain colour string, or the
output of a \verb{fill_*()} helper -- \code{\link[=fill_solid]{fill_solid()}}, \code{\link[=fill_none]{fill_none()}},
\code{\link[=fill_hatch]{fill_hatch()}}, \code{\link[=fill_crosshatch]{fill_crosshatch()}}, \code{\link[=fill_stipple]{fill_stipple()}}, \code{\link[=fill_noise]{fill_noise()}},
\code{\link[=fill_gradient]{fill_gradient()}}, or \code{\link[=fill_vignette]{fill_vignette()}}. Default \code{fill_solid("black")}
(i.e. \code{"black"}).}

\item{linewidth}{Line width. Default \code{1}.}

\item{linetype}{Line dash pattern, forwarded to \code{\link[grid:gpar]{grid::gpar()}}'s \code{lty}.
Either a named type (\code{"solid"}, \code{"dashed"}, \code{"dotted"}, \code{"dotdash"},
\code{"longdash"}, \code{"twodash"}, \code{"blank"}), an integer code \code{0:6}, or a
custom hex dash-pattern string (e.g. \code{"13"}) -- see \code{\link[grid:gpar]{grid::gpar()}} and
\code{graphics::par()}'s \code{lty} for the full set of accepted forms, which
aren't independently re-validated here. Default \code{"solid"}.}

\item{linejoin}{Line join style at each vertex, forwarded to
\code{\link[grid:gpar]{grid::gpar()}}'s \code{linejoin}. One of \code{"round"}, \code{"mitre"}, or \code{"bevel"}.
Most visible on closed shapes with few, sharp vertices, or on any
drawable stroked with a thick \code{linewidth}. Default \code{"round"}.}

\item{lineend}{Line end style at a path's free endpoints, forwarded to
\code{\link[grid:gpar]{grid::gpar()}}'s \code{lineend}. One of \code{"round"}, \code{"butt"}, or \code{"square"}.
Only visible on \code{"path"}-geometry drawables (e.g. \code{\link[=curve_line]{curve_line()}},
\code{\link[=curve_bezier]{curve_bezier()}}) -- a \code{"polygon"}-geometry drawable has no free
endpoint, since its outline closes back on itself. Most visible at a
thick \code{linewidth}. Default \code{"round"}.}

\item{linemitre}{Mitre limit, forwarded to \code{\link[grid:gpar]{grid::gpar()}}'s \code{linemitre}.
Only takes effect when \code{linejoin = "mitre"}: at a vertex sharper than
this limit allows, the mitred corner is truncated to a bevel instead,
to avoid an arbitrarily long spike. Must be at least \code{1}. Default \code{10},
matching \code{\link[grid:gpar]{grid::gpar()}}'s own default.}

\item{rule}{Fill rule used when a drawable's own \code{points} has more than
one sub-path (see \link{xy}'s \code{id}), forwarded to \code{\link[grid:pathGrob]{grid::pathGrob()}}'s own
\code{rule} argument. One of \code{"evenodd"} (default) or \code{"winding"}.
\code{"evenodd"} fills a region if it's enclosed by an odd number of
sub-paths, regardless of each sub-path's own vertex winding direction
-- a sub-path nested inside another becomes a hole purely from
geometric nesting, with no need to get vertex order right by hand,
which is why it's the default. \code{"winding"} instead fills based on net
signed winding number, which depends on each sub-path's own direction
-- only useful for constructions that specifically need that
direction-sensitive behavior. Has no effect on a drawable with only
one implicit sub-path (every current \verb{shape_*()}/\verb{curve_*()}
constructor), since both rules agree there.}

\item{color_alpha}{Stroke opacity, applied to \code{color} independently of
\code{fill_alpha}. Must be a number in \verb{[0, 1]}, where \code{0} is fully
transparent and \code{1} (the default) is fully opaque. Applied by baking
the value into \code{color} via \code{\link[grDevices:adjustcolor]{grDevices::adjustcolor()}} at draw time
(see \code{\link[=draw]{draw()}}'s internal \code{apply_alpha()} helper), not via
\code{\link[grid:gpar]{grid::gpar()}}'s own \code{alpha} argument -- \code{gpar()}'s \code{alpha} applies
uniformly to both stroke and fill on the same grob, which would
couple \code{color_alpha} and \code{fill_alpha} together. If \code{color} already
has its own alpha channel (e.g. an \code{"#RRGGBBAA"} hex string),
\code{color_alpha} multiplies through it rather than overriding it.}

\item{fill_alpha}{Fill opacity, applied to \code{fill} independently of
\code{color_alpha}, via the same \code{\link[grDevices:adjustcolor]{grDevices::adjustcolor()}} mechanism as
\code{color_alpha}. Must be a number in \verb{[0, 1]}. Default \code{1}. Only has an
effect when \code{fill} is a plain colour string (as from \code{\link[=fill_solid]{fill_solid()}}
or \code{\link[=fill_none]{fill_none()}}) -- \strong{silently inert when \code{fill} is a pattern or
gradient} (the output of any other \verb{fill_*()} helper), since
\code{\link[grDevices:adjustcolor]{grDevices::adjustcolor()}} has no defined effect on a \code{GridPattern}
object. This mirrors \code{fill} itself already having no effect for
\code{"path"}/\code{"points"}-geometry drawables (see \link{drawable}'s \code{geometry}
docs), and \code{lineend}/\code{linemitre} already being inert for some
geometries -- geometry- or fill-type-conditional inertness, not an
error, is this package's existing convention for style properties
that don't universally apply.}
}
\description{
\code{style} is a container for the graphical properties passed to
\code{\link[grid:gpar]{grid::gpar()}} when a \link{drawable} object is drawn.
}
\examples{
style(color = "steelblue", fill = "lightblue", linewidth = 2)
style(fill = fill_hatch(angle = 30))

# linejoin/linemitre are most visible on a thick-stroked shape with a
# sharp vertex
star <- shape_polygon(n = 5, radius = 1, fill = "white")
draw(shape_stroke(
  x = star@points@x, y = star@points@y, width = 0.25,
  linejoin = "mitre", linemitre = 1.5
))

# color_alpha/fill_alpha control stroke/fill opacity independently
draw(shape_circle(
  radius = 1, color = "black", fill = "tomato",
  color_alpha = 1, fill_alpha = 0.3, linewidth = 3
))

# lineend only affects a path's free endpoints, not a closed polygon
draw(curve_line(
  x = c(0, 1, 2), y = c(0, 1, 0), linewidth = 15, lineend = "square"
))

}
\seealso{
Other core structure:
\code{\link[=canvas]{canvas()}},
\code{\link[=draw]{draw()}},
\code{\link[=drawable]{drawable()}},
\code{\link[=sketch]{sketch()}},
\code{\link[=xy]{xy()}}
}
\concept{core structure}
