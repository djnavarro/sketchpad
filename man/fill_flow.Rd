% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill.R
\name{fill_flow}
\alias{fill_flow}
\title{Domain-warped noise texture fill}
\usage{
fill_flow(
  color = "black",
  spacing = 0.5,
  aspect = 1,
  resolution = 32L,
  alpha = 1,
  warp = 2,
  warp_frequency = 1,
  warp_octaves = 1L,
  frequency = 1,
  octaves = 2L,
  seed = 1L,
  extend = "repeat"
)
}
\arguments{
\item{color}{One or more fill colours. A single colour (the default)
fades in opacity from fully transparent to \code{alpha}, exactly as before
this argument accepted a vector. Two or more colours instead blend
across a \code{grDevices::colorRamp()} built from them, driven by the noise
value, with \code{alpha} applied as a flat opacity across the whole fill
(see the internal \code{noise_to_pixels()} helper). Default \code{"black"}.}

\item{spacing}{Baseline tile size, as a fraction of the target's bounding
box. Must be a positive number. Default \code{0.5}.}

\item{aspect}{Width-to-height ratio of the target polygon's bounding box.
Must be a positive number. Default \code{1} (a square bounding box).}

\item{resolution}{Raster resolution (pixels per tile edge). Must be a
positive integer of at least \code{2L}. Default \code{32L}.}

\item{alpha}{Opacity. For a single \code{color}, the maximum opacity at the
noise field's peak; for two or more, a flat opacity applied uniformly.
Must be a number in \verb{(0, 1]}. Default \code{1}.}

\item{warp}{Warp amplitude, in radians of angular displacement. Must be
a non-negative number. Default \code{2}.}

\item{warp_frequency}{Frequency of the two warp-displacement noise
fields. Must be non-negative. Default \code{1}.}

\item{warp_octaves}{Number of octaves for the warp-displacement noise
fields. Must be a positive integer. Default \code{1L}.}

\item{frequency}{Noise frequency, as in \code{\link[=shape_blob]{shape_blob()}}. Must be non-negative.
Default \code{1}.}

\item{octaves}{Number of noise octaves, as in \code{\link[=shape_blob]{shape_blob()}}. Must be a
positive integer. Default \code{2L}.}

\item{seed}{Integer seed for the noise field, as in \code{\link[=shape_blob]{shape_blob()}}. Default
\code{1L}.}

\item{extend}{Passed to \code{\link[grid:pattern]{grid::pattern()}}. Default \code{"repeat"}.}
}
\value{
A pattern object as returned by \code{\link[grid:pattern]{grid::pattern()}}, suitable for
use as the \code{fill} argument to \code{\link[grid:gpar]{grid::gpar()}}.
}
\description{
\code{fill_flow()} is a swirlier variant of \code{\link[=fill_noise]{fill_noise()}}: before sampling
the final noise field, the tile's own \verb{(theta_u, theta_v)} angles (see
\code{\link[=fill_noise]{fill_noise()}} details for why angles, not raw \verb{(u, v)}) are displaced
by a second, independent torus-periodic noise field -- the "fBm of fBm"
domain-warping recipe popularized for flowing, curl-noise-like
textures, adapted here so the warp field is itself torus-periodic
rather than sampled directly, keeping the whole result seamless.
}
\details{
The warp field needs to be decorrelated from the final field and from
itself along each axis, but \code{ambient::gen_simplex()}'s 4 input
dimensions are already fully spent on the \verb{(theta_u, theta_v)} torus
trick, leaving no spare dimension to offset. \code{fill_flow()} decorrelates
by seed instead: the \code{u}- and \code{v}-displacement fields are sampled at
\code{seed + 104729L} and \code{seed + 200003L} respectively (arbitrary large
primes, chosen only to make collisions with a user's own nearby seed
choices unlikely), before the final field is sampled at \code{seed} itself.

Displacing a periodic field's own periodic coordinates by another
periodic field preserves periodicity: shifting \code{u} from \code{0} to \code{1}
still advances \code{theta_u} by exactly \code{2 * pi} (a full turn), and the
warp added at each end is identical since it's sampled from a field
that's periodic in \code{u} itself -- so the warped angle wraps around
exactly as the unwarped one did, with no seam.

As with \code{\link[=fill_noise]{fill_noise()}}, a faint tile-boundary rasterization seam can
still be visible on some devices despite the field being exactly
periodic (see its details); larger \code{warp} values tend to make this
more noticeable, for the same reason described at \code{\link[=fill_marble]{fill_marble()}}.
}
\examples{
draw(shape_circle(fill = fill_flow(warp = 3, seed = 9350L)))

# a larger warp gives a swirlier, more curl-noise-like look; warp = 0
# reduces to plain fill_noise()
draw(shape_circle(fill = fill_flow(warp = 0, seed = 9350L)))
draw(shape_circle(fill = fill_flow(warp = 6, seed = 9350L)))

# two or more colours blend across the field, as in fill_noise()
draw(shape_circle(
  fill = fill_flow(color = c("steelblue", "white", "tomato"), seed = 9350L)
))

}
\seealso{
Other fill helpers:
\code{\link[=fill_charcoal]{fill_charcoal()}},
\code{\link[=fill_checker]{fill_checker()}},
\code{\link[=fill_crosshatch]{fill_crosshatch()}},
\code{\link[=fill_gradient]{fill_gradient()}},
\code{\link[=fill_halftone]{fill_halftone()}},
\code{\link[=fill_hatch]{fill_hatch()}},
\code{\link[=fill_image]{fill_image()}},
\code{\link[=fill_marble]{fill_marble()}},
\code{\link[=fill_noise]{fill_noise()}},
\code{\link[=fill_none]{fill_none()}},
\code{\link[=fill_scatter]{fill_scatter()}},
\code{\link[=fill_scribble]{fill_scribble()}},
\code{\link[=fill_solid]{fill_solid()}},
\code{\link[=fill_stipple]{fill_stipple()}},
\code{\link[=fill_stripe]{fill_stripe()}},
\code{\link[=fill_vignette]{fill_vignette()}}
}
\concept{fill helpers}
